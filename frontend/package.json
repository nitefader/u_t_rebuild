{
  "name": "trading-os-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "build": "vite build && node scripts/check-frontend.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 5173",
    "test": "node --test tests/*.test.mjs && node scripts/check-frontend.mjs"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
