# Cross-Boundary Change Ledger

Append-only. Newest entry on top. Format and rules: `COORDINATION/PROTOCOL.md`.

One line per change that crosses the operation boundary
(frontend â†” backend) or that the other agent should know about before
their next push.


---
- 2026-04-30 04:00:00 -04:00 · Claude · backend-internal+frontend-consumed · `backend/app/orders/manager.py`, `backend/app/pipeline/{orchestrator,models}.py`, `backend/app/operations/{models,service}.py`, `backend/tests/unit/{orders,pipeline,operations}/test_*post_fill*.py` (3 new files), `backend/tests/unit/orders/test_order_manager_native_bracket.py`, `backend/tests/unit/operations/test_account_operations_position_views.py`, `backend/tests/unit/pipeline/test_runtime_orchestrator_post_fill_bracket_critic_fixes.py`, `frontend/src/api/schemas/operations.ts`, `frontend/src/lib/protectionDisplay.{ts,test.ts}`, `frontend/src/routes/{AccountDetailDrawer,OperationsLedger}.tsx`, `frontend/src/routes/AccountDetailDrawer.test.tsx` — **T-5 Bracket Program (orchestrator wiring + protection-status surface + adversarial critic-fix bundle).** RuntimeOrchestrator now (1) passes `execution_plan=` into SignalPlanBuilder so T-3 bracket-intent encoding actually fires in production, (2) attaches native broker bracket child prices on the entry InternalOrder when execution_mode=`native_alpaca_bracket`, and (3) on every SignalPlan-origin OPEN entry fill calls ProtectiveOrderPlacer → OrderManager.create_protective_orders_post_fill → BrokerAdapter for the post-fill stop+target children. New `OrderManager` methods: `create_protective_orders_post_fill(plan, parent_order)` (lineage-correct child orders with `order_class="oco"` and breakpoint-suffixed leg labels for partial-fill uniqueness) and `attach_native_bracket_to_entry(...)` (idempotent native bracket attachment on the entry leg, raises on conflicting re-attach). `cumulative_covered_qty_for_signal_plan` made public + filters CANCELED/REJECTED/FAILED stops so a rejected stop doesn't permanently inflate `already_covered_qty`. New pipeline event types `PROTECTION_PLACED` / `PROTECTION_NAKED` (rule_id `protection_failed_after_fill`). New operator-facing `OperatorPositionView { snapshot, protection_status, protective_order_count }` shipped on `AccountOperations.position_views`; service joins by `opening_signal_plan_id` to compute `protected | pending_protection | naked | unknown`. Frontend Open Positions tables (per-account drawer + all-accounts ledger) gain a "Protection" column with tone-coded `StatusBadge` and tooltips driven by a single shared `getProtectionDisplay` helper. Three parallel sonnet critics (architecture / adversarial / UX) ran at closeout; 9 fix-in-slice items shipped: PROTECTION_PLACED only fires on at-least-one-success + parent-keyed PROTECTION_NAKED with reason `all_children_rejected` when zero succeed; stop-leg rejection aborts the loop (no target-only "protection"); ledger's cumulative `filled_quantity` used instead of broker_result delta; native bracket double-call is idempotent on identical prices and rejects on different; post-fill skipped when entry already has `order_class="bracket"`; AccountDetailDrawer row key uses `position_lineage_id` (collision fix); unknown enum statuses render with `warn` tone (no silent neutral); shared `getProtectionDisplay` helper with operator tooltips. Verification: `pytest backend/tests/unit -q` → **1562 passed** (+5 new critic-fix tests over T-5 baseline 1557; +31 over T-4 baseline 1531; +186 over original Bracket Program baseline 1376); `npx tsc --noEmit` clean; `npx vitest run` → **399 passed across 54 test files** (+7 over T-4 baseline 392); `npm run lint:names` clean. MAP: `Operations_Turtle_Shell_Artifacts/STRATEGY_TO_BROKER_BRACKET_PROGRAM.md`. Log: `docs/agent_logs/2026-04-29_21-52-19_bracket_execution.md` (Pass 5 + Pass 6).
- 2026-04-29 23:35:00 -04:00 · Claude · backend-internal · `backend/app/orders/protective_placer.py`, `backend/app/orders/models.py`, `backend/app/brokers/alpaca.py`, `backend/tests/unit/orders/test_protective_placer.py`, `backend/tests/unit/brokers/test_alpaca_native_bracket.py` — **T-4 Bracket Program (order execution dual-mode — components landed).** New `ProtectiveOrderPlacer` decodes SignalPlan post_fill_pct intent + a BrokerSync fill event into a `ProtectivePlacementPlan` with concrete prices (LONG/SHORT symmetric, exit side flipped, partial-fill idempotent). `InternalOrder` gains `bracket_take_profit_limit_price` + `bracket_stop_loss_stop_price` optional fields for native bracket. `AlpacaBrokerAdapter.to_alpaca_order_request` now attaches `OrderClass.BRACKET` + `TakeProfitRequest` + `StopLossRequest` when `order.order_class == "bracket"`; `_validate_native_bracket_preflight` raises `AlpacaBrokerError` with structured codes (`native_bracket_missing_child_prices`, `native_bracket_unsupported_tif`, `native_bracket_unsupported_extended_hours`, `native_bracket_unsupported_fractional`) per operator directive *"fail clearly if Alpaca rejects the structure."*. `AlpacaBrokerCapabilities.supports_brackets` flipped from false to true. 24 new tests (12 ProtectiveOrderPlacer + 12 AlpacaBrokerAdapter native bracket). T-5 will wire these into the orchestrator + OrderManager + Operations UI. Verification: `pytest backend/tests/unit -q` → **1531 passed** (+25 over T-3 baseline 1506; +136 over original 1395). MAP: `Operations_Turtle_Shell_Artifacts/STRATEGY_TO_BROKER_BRACKET_PROGRAM.md`. Log: `docs/agent_logs/2026-04-29_21-52-19_bracket_execution.md`.
- 2026-04-29 23:10:00 -04:00 · Claude · backend-internal · `backend/app/decision/signal_plan_builder.py`, `backend/tests/unit/decision/test_signal_plan_builder_bracket_intent.py` — **T-3 Bracket Program (SignalPlan enrichment).** `SignalPlanBuilder.build_from_candidate(...)` accepts a new optional `execution_plan: ExecutionStyleVersion`. When present and the preset is a bracket variant (`BracketStopTargetPreset` / `BracketRunnerPreset` / `MultiTargetScaleOutPreset`), the builder populates `SignalPlan.stop` and `SignalPlan.targets` with the operator-declared bracket intent, encoded as `post_fill_pct:<pct>` for post-fill resolution by T-4's ProtectiveOrderPlacer. New helpers: `post_fill_pct_rule()` encoder + `parse_post_fill_pct()` decoder. Long/short symmetry verified — same percent intent on both sides. SignalPlan remains neutral and quantity-free (doctrine guard tested). Legacy `candidate.stop_candidate` / `candidate.target_candidate` path preserved for backwards-compat with non-bracket callers. 9 new tests covering all four bracket preset shapes + symmetry + doctrine guards + legacy fallback. Verification: `pytest backend/tests/unit -q` → **1506 passed** (+9 over T-2 baseline 1497). MAP: `Operations_Turtle_Shell_Artifacts/STRATEGY_TO_BROKER_BRACKET_PROGRAM.md`. Log: `docs/agent_logs/2026-04-29_21-52-19_bracket_execution.md`.
- 2026-04-29 22:50:00 -04:00 · Claude · frontend-consumed · `frontend/src/api/schemas/strategyComposer.ts`, `frontend/src/components/strategy_builder/editor/{editorState.ts,Page2TabShell.tsx,sections/ExecutionPresetSection.tsx}`, `frontend/src/routes/StrategyCompose.tsx` — **T-2 Bracket Program (Compose/API wiring).** `ExecutionMode` enum exposed via zod schema; new operator-visible execution_mode selector on the Stop·Target·Execution tab's ExecutionPresetSection card with default `post_fill_bracket` + optional `native_alpaca_bracket`. `derivePresetFromWizard()` replaces the hardcoded `market_entry_market_exit` so wizard intent (has_runner / has_multiple_targets / has_stop+has_target) drives the AI compose seed. `applyExecutionModeToDraft` + `readExecutionMode` editor-state helpers. 13 new vitest tests (5 derivePresetFromWizard + 5 editorState execution_mode + 3 ExecutionPresetSection). Verification: `npx tsc --noEmit` clean; `npm test --run` → **392 passed across 52 files** (+13 over baseline 379); `npm run lint:names` clean; backend `pytest backend/tests/unit -q` → **1497 passed** (no regression). MAP: `Operations_Turtle_Shell_Artifacts/STRATEGY_TO_BROKER_BRACKET_PROGRAM.md`. Log: `docs/agent_logs/2026-04-29_21-52-19_bracket_execution.md`.
- 2026-04-29 22:30:00 -04:00 · Claude · schema-added+route-internal · `backend/app/strategy_controls/`, `backend/app/execution_plans/`, `backend/app/deployments/{models,service}.py`, `backend/app/domain/execution_style.py`, `backend/app/strategy_composer/service.py`, `backend/app/api/routes/strategies.py`, `backend/tests/unit/{strategy_controls,execution_plans,strategy_composer}/` — **T-1 Bracket Program (persistence trio).** New saved entities `strategy_controls_versions` and `execution_plan_versions` (operator-approved per resume prompt 2026-04-29). New columns/FKs on `Deployment`: `strategy_controls_version_id`, `execution_plan_version_id`, `risk_plan_version_id`. New `ExecutionMode` enum on `ExecutionStyleVersion` (`post_fill_bracket` default + `native_alpaca_bracket` optional). `StrategyComposerService.save_draft` now persists both controls + plan and returns the version ids; the strategies API route wires both repos against the runtime DB. New repo classes `StrategyControlsRepository` and `ExecutionPlanRepository` (immutable rows, `(id, version)` UNIQUE). 21 new tests (12 repo unit + 3 composer acceptance + 6 reload-survival across processes). Doctrine: `MY_COMMAND_EXECUTION_PLAN_PERSISTENCE_AND_LABS.md` lines 159-164 lock that the Deployment binds StrategyVersion + ControlsVersion + ExecutionPlanVersion — FKs go on `deployments`, not on `strategy_versions`. Verification: `pytest backend/tests/unit -q` → **1497 passed** (+21 over baseline 1476). Zero regressions. MAP: `Operations_Turtle_Shell_Artifacts/STRATEGY_TO_BROKER_BRACKET_PROGRAM.md`. Log: `docs/agent_logs/2026-04-29_21-52-19_bracket_execution.md`.
- 2026-04-29 20:15:00 -04:00 · Claude · backend-internal+frontend-consumed · `backend/app/api/routes/broker_accounts.py`, `backend/app/persistence/runtime_store.py`, `backend/tests/unit/api/test_broker_accounts_routes.py`, `frontend/src/components/risk_plans/HorizonRiskPlanPicker.tsx`, `frontend/src/routes/{RiskCardPanel,Deployments,EditDeploymentDrawer,Operations}.{tsx,test.tsx}` — **Slice B adversarial fixes (Governor Wiring follow-up).** Two parallel adversarial recursions (sonnet ×2) on the Slice B output identified 4 BUG/RISK + 5 NIT findings; 11 fix-in-slice items shipped. Backend (3): (1) B-BUG-1 — PUT `/risk-plan-map` now validates `risk_plan_version_id` exists in `risk_plan_versions` and returns 400 on dangling reference (was silently writing dangling FK that caused permanent rejection with no operator signal); (2) B-RISK-2 — `load_account_risk_plan_map` defensively skips rows with unknown TradingHorizon enum values (was crashing whole-account map load on a single corrupt row); (3) B-RISK-3 — `load_risk_plan_config_for_horizon` filters out `RiskPlanVersionStatus.DEPRECATED` (was silently still applying retired plans). Frontend (8): F-BUG-1 archived-plan staleness chip+warning on `HorizonRiskPlanPicker`; F-BUG-2 ref-based mutation lock + post-flight queue drain prevents concurrent multi-horizon edit race; F-RISK-1 "0 of 5 horizons covered" danger banner on Account Risk Card; F-RISK-2 a11y — dropped redundant `aria-label`, label text now self-describes ("Swing risk plan"), `aria-describedby` wires explainer to the dropdowns; F-RISK-3 Operations test for `account_missing_risk_plan_for_horizon` friendly text; F-RISK-4 + B-RISK-1 Deployments test for risk_horizon picker + "Per-horizon RiskPlan enforcement is OFF" warning when blank (in both Deployments.tsx and EditDeploymentDrawer.tsx); F-NIT-1 tightened `resolveVersionId` types (no `!`); F-NIT-2 banner copy now cites the rule_id by name. Verification: backend `pytest backend/tests/unit -q` → **1476 passed** (+1 new B-BUG-1 regression test); frontend `npm test` → **379 passed** across 51 test files (+1 new Operations test); `npx tsc --noEmit` clean. Zero remaining BUG-severity findings from the adversaries.
- 2026-04-29 20:30:00 -04:00 · Claude · schema-added · `backend/app/persistence/models.py` — Added `account_risk_plan_map` table (PRIMARY KEY `account_id, horizon`); broker account deletion now cascades to this table. `backend/app/broker_accounts/risk_plan_map_models.py` (new file): `AccountRiskPlanMapEntry`, `AccountRiskPlanMap`, `AccountRiskPlanMapUpdateRequest` Pydantic models. `backend/app/persistence/runtime_store.py`: four new methods — `load_account_risk_plan_map`, `save_account_risk_plan_map_entry`, `delete_account_risk_plan_map_entry`, `load_risk_plan_config_for_horizon` (joins via `risk_plan_versions`).
- 2026-04-29 20:30:00 -04:00 · Claude · route-added · `GET /api/v1/broker-accounts/{account_id}/risk-plan-map` and `PUT /api/v1/broker-accounts/{account_id}/risk-plan-map` — Added to `backend/app/api/routes/broker_accounts.py`. GET returns `AccountRiskPlanMap` (empty entries when unmapped); PUT with `risk_plan_version_id=null` deletes the horizon row; PUT with a UUID upserts. Both 404 on unknown account. CONTRACT for frontend agent: paths, shapes, and enums as above.
- 2026-04-29 20:30:00 -04:00 · Claude · backend-internal · **Slice B backend (Governor Wiring — AccountRiskPlanMap + horizon enforcement).** Delivers: (1) `TradingHorizon.OTHER = "other"` added to enum; (2) `DeploymentContext.risk_horizon: TradingHorizon | None = None`; (3) `account_risk_plan_map` table + runtime_store CRUD + join-based `load_risk_plan_config_for_horizon`; (4) two new broker-account API routes (see route-added entry); (5) production `_plan_lookup` in `BrokerRuntimeOrchestrator._build_governor_policy_resolver()` now calls `runtime_store.load_risk_plan_config_for_horizon`; (6) `GovernorPolicy.requires_risk_plan: bool = False`; `GovernorPolicyResolver.resolve()` gains `enforce_plan_required: bool = False` — set True by orchestrator only when Deployment has explicit risk_horizon; (7) `PortfolioGovernor.evaluate()` rejects with `rule_id="account_missing_risk_plan_for_horizon"` when `active_policy.requires_risk_plan is True`; (8) 42 new backend tests. Verification: full `pytest backend/tests/unit -q` → **1475 passed** (was 1433, +42). Boundary suite `pytest backend/tests/unit/api/test_frontend_api_contract.py backend/tests/unit/lint -q` → 230 passed, 0 failures.
- 2026-04-29 19:15:00 -04:00 · Claude · backend-internal · `backend/app/governor/policy_resolver.py`, `backend/app/governor/__init__.py`, `backend/app/governor/service.py`, `backend/app/pipeline/orchestrator.py`, `backend/app/runtime/account_trading_orchestrator.py`, `backend/app/broker_accounts/models.py`, `backend/app/domain/signal_plan.py`, `backend/tests/unit/governor/test_policy_resolver.py`, `backend/tests/unit/governor/test_portfolio_governor.py`, `backend/tests/unit/pipeline/test_runtime_orchestrator.py`, `Operations_Turtle_Shell_Artifacts/GOVERNOR_WIRING_MAP.md`, `Operations_Turtle_Shell_Artifacts/GOVERNOR_WIRING_MAP_SLICE_B.md` — **Slice A: Governor numeric-limits wiring (operator override).** Closes the silent-no-op hole where AccountRiskConfig and RiskPlanConfig numeric limits never gated a single order in production. New `GovernorPolicyResolver` translates `(account_id, TradingHorizon) -> GovernorPolicy` per evaluation with min-of-floor-and-Account-and-Plan rule and verbatim kill/pause preservation; field-name asymmetry `max_symbol_concentration_pct` ↔ `max_symbol_exposure_pct` resolved by the resolver. `PortfolioGovernor.evaluate(request, *, policy_override=None)` accepts a per-call override without mutating `self._policy`. `RuntimeOrchestrator` accepts `governor_policy_resolver=` kwarg; `BrokerRuntimeOrchestrator._build_governor_policy_resolver` wires the runtime_store-backed `AccountRiskConfig` lookup in production (RiskPlanConfig lookup is a no-op until Slice B's `AccountRiskPlanMap` lands). **Risk Horizon doctrine LOCKED**: *Deployment chooses horizon. Account chooses risk plan. Governor enforces.* Per-deployment horizon today reads `Deployment.risk_horizon` (Slice B field — falls back to `StrategyControls.trading_horizon`). Adversarial fixes shipped: (1) `AccountRiskConfig.max_open_positions` default 5→None (was a silent 5-position cap on every default account); (2) `GovernorDecisionTrace.projected_state: dict[str, object] | None` field added so operator sees the gate's numeric snapshot (`new_open_slots_remaining`, projected exposures, broker stale flag); (3) production composition wires the resolver. Verification: `pytest backend/tests/unit/governor backend/tests/unit/pipeline backend/tests/unit/broker_accounts backend/tests/unit/runtime -q` → 206 passed; full `pytest backend/tests/unit -q` → **1433 passed** (was 1431, +2 from new tests; +38 over pre-slice baseline 1395). Two parallel adversarial recursions (sonnet ×2): zero remaining BUG-severity findings. **Slice B (AccountRiskPlanMap entity + TradingHorizon.OTHER + Deployment.risk_horizon + frontend per-horizon UI) queued and gated on Angry Architect approval — see GOVERNOR_WIRING_MAP_SLICE_B.md.** Heads-up filed in INBOX_CODEX 2026-04-29 18:42:51.
- 2026-04-29 16:34:21 -04:00 · Codex · frontend-consumed · `frontend/src/components/ui/HoldToArmConfirm.tsx`, `frontend/src/routes/{Deployments,Watchlists}.tsx` — Operator rejected typed bulk-delete confirmation and asked for a two-second press-and-hold verifier with optional notes. Added reusable hold-to-arm dialog and wired Deployment bulk/single delete plus Watchlist bulk archive/delete and detail delete to hold for two seconds, pulse while holding, turn green when verified, then enable the final action. Existing backend delete/archive guards remain the source of truth; optional notes are UI-only because these delete/archive routes do not accept note bodies today.
- 2026-04-29 16:34:21 -04:00 · Codex · lint · `frontend/src/components/ui/HoldToArmConfirm.test.tsx`, `frontend/src/routes/{Deployments,Watchlists}.test.tsx` — Verification passed: frontend `npm.cmd run typecheck`; focused vitest `src/components/ui/HoldToArmConfirm.test.tsx src/routes/Deployments.test.tsx src/routes/Watchlists.test.tsx` -> 13 passed; `npm.cmd run lint:names` clean; `git diff --check` clean with CRLF warnings only. No backend/trading-spine path changed.
- 2026-04-29 16:07:27 -04:00 · Codex · frontend-consumed · `frontend/src/routes/{Screeners,ScreenerDetail}.tsx`, `frontend/src/components/screener/DiscoveryScheduleControls.tsx` — Operator flagged confusion around scheduled Screener runs, template editability, duplicate-looking templates, and Alpaca Market Lists. Shipped frontend clarity pass: saved Screener cards show schedule state/next automatic run/direct Schedule action; schedule card says active schedules run by themselves and Run schedule now is manual; Alpaca Market Lists are labeled live provider runs, not templates, with intent icons and up-to-50-symbol metadata; template drawer cards show intent, universe size, sample symbols, timeframe, rule count, and sort metric; `Duplicate version` renamed to `Customize version`; flat template logic can become editable criteria in a new version while complex AI trees stay preserved. Verification: frontend typecheck passed; focused vitest 12 passed; `lint:names` clean; `git diff --check` clean with CRLF warnings only. No backend or trading-spine path changed.
- 2026-04-29 15:57:06 -04:00 · Codex · frontend-consumed · `frontend/src/routes/Screeners.tsx`, `frontend/src/routes/Screeners.test.tsx` — Operator clarified that inline Screener templates were confusing because they looked like actual Screeners/Watchlists. Moved templates out of the main Screeners page into a `Browse templates` drawer, with copy stating templates create Screeners and are not Watchlists or Deployment attachments. Verification: frontend typecheck passed; `npx.cmd vitest run src/routes/Screeners.test.tsx` passed 6 tests; `git diff --check` clean with CRLF warnings only. No backend or trading-spine path changed.
- 2026-04-29 14:14:35 -04:00 · Codex · frontend-consumed · `frontend/src/routes/{Screeners,ScreenerDetail,Watchlists}.tsx`, `frontend/src/components/screener/{ResultsTable,UniverseSourcePicker}.tsx`, `Operations_Turtle_Shell_Artifacts/SCANNER_WATCHLIST_BEST_PRACTICES_CLEANUP.md` — Operator asked to work with `TradingFirmScannerExpert.agent.md` and go online for scanner/watchlist best practices. Shipped frontend-only cleanup: saved-Screener and Watchlist search/filter/sort, visible-set Watchlist bulk selection, clearer matched-results save action, readable run/status/source/evidence labels, dynamic Watchlist snapshot copy, Alpaca market-list provider failure state, and source-backed MAP artifact. Verification: frontend typecheck passed; focused screener/watchlist vitest 20 passed; `git diff --check` clean with CRLF warnings only. No trading-spine, Account truth, BrokerSync, SignalPlan, or order path changed.
- 2026-04-29 13:52:03 -04:00 · Codex · frontend-consumed · `frontend/src/components/strategy_builder/editor/sections/StrategyControlsSection.tsx`, `frontend/src/api/schemas/strategyComposer.test.ts`, `frontend/src/components/strategy_builder/editor/coherenceValidator.ts` — Final worktree cleanup included Strategy Controls cooldown/caps UI, schema coverage for the expanded controls payload, and a coherence warning for cooldown bars on daily-or-coarser strategies. Verification: frontend `npm.cmd run typecheck` passed; focused Strategy Controls / Strategy Composer / coherence validator vitest 68 passed; `git diff --check` clean with CRLF warnings only. Proceeding to commit and push final cleanup bundle.
- 2026-04-29 13:46:55 -04:00 · Codex · git · final release cleanup — Pushed final Strategy Composer schema cleanup and operation closeout to `origin/master`. All known dirty/untracked work from the requested cleanup has now been committed and pushed.
- 2026-04-29 13:46:15 -04:00 · Codex · frontend-consumed · `frontend/src/api/schemas/strategyComposer.ts` — Included final dirty schema work in release cleanup: StrategyControls schema now accepts session windows, no-new-entry/force-flat times, time-based exit limits, cooldown/trade caps, feature refs, and regime filter refs with client-side validation mirroring backend constraints. Verification: frontend `npm.cmd run typecheck` passed; focused Strategy Compose / StrategyControls / coherence validator vitest 54 passed.
- 2026-04-29 13:44:32 -04:00 · Codex · git · `3c5505a` — Release cleanup commit `Ship account risk routes and screener UX polish` pushed to `origin/master`. Commit includes AccountRiskConfig/AccountRestrictions persistence + routes, Risk Card live wiring, Deployment/Watchlist bulk actions, Screener/Watchlist UX clarity plan and tests, and coordination closeout.
- 2026-04-29 13:43:29 -04:00 · Codex · coordination · release cleanup — Operator requested all work committed and pushed. Verification before commit: `git diff --check` clean with CRLF warnings only; frontend `npm.cmd run typecheck` passed; frontend `npx.cmd vitest run --testTimeout=15000` passed 48 files / 336 tests; frontend `npm.cmd run lint:names` clean; backend `python -m pytest backend/tests/unit -q` passed 1395 tests, 6 warnings. Proceeding to stage all dirty/untracked work and push `master` to `origin`.
- 2026-04-29 13:31:19 -04:00 · Codex · operations-fix · local backend/frontend dev servers — Restarted stale local uvicorn/Vite processes so Account Detail Risk Card loads the newly registered broker-account risk routes. Verified direct backend and Vite-proxied HTTP 200 for `GET /api/v1/broker-accounts/e43733eb-4d90-473b-af46-6aaac06e85f7/risk-config` and `/restrictions`. Active listeners after cleanup: backend `127.0.0.1:8000`, frontend `127.0.0.1:5173`.
- 2026-04-29 13:00:42 -04:00 · Codex · route-added · `GET/PUT /api/v1/broker-accounts/{account_id}/risk-config` and `GET/PUT /api/v1/broker-accounts/{account_id}/restrictions` — Account Detail Risk Card placeholder is now backed by durable AccountRiskConfig and AccountRestrictions rows. GET returns a readable default for existing accounts; PUT persists a full account-scoped config/restriction document with version and updated_at. This is Account policy data only; no Deployment state, broker truth, or order path changed.
- 2026-04-29 13:00:42 -04:00 · Codex · schema-added · `backend/app/broker_accounts/models.py`, `backend/app/persistence/{models.py,runtime_store.py}` — Added AccountRiskConfig and AccountRestrictions Pydantic models plus SQLite persistence. Broker account deletion now also removes those account-owned policy rows.
- 2026-04-29 13:00:42 -04:00 · Codex · frontend-consumed · `frontend/src/routes/{RiskCardPanel,Deployments,Watchlists}.tsx`, `frontend/src/api/schemas/risk.ts`, `frontend/src/routes/explainerContent.ts` — Risk Card no longer says Operation Turtle Shell route is pending. Deployments now support list-level multi-select bulk delete through the existing guarded delete route; Watchlists now support list-level multi-select archive and guarded hard delete. Rows keep readable names as the primary labels and report per-row blockers.
- 2026-04-29 13:00:42 -04:00 · Codex · lint · `backend/tests/unit/api/test_broker_accounts_routes.py`, `backend/tests/unit/persistence/test_sqlite_persistence.py`, `backend/tests/unit/api/test_frontend_api_contract.py`, `frontend/src/routes/{Deployments,Watchlists,RiskCardPanel}.test.tsx` — Verification passed: backend focused route/persistence/contract tests 41 passed; frontend `npm.cmd run typecheck` passed; focused vitest 12 passed. Existing React Router future-flag and Radix ref warnings remain test-environment warnings. Leases released and Claude inbox notified.
- 2026-04-29 08:06:58 -04:00 · Codex · frontend-consumed · `frontend/src/components/screener/`, `frontend/src/routes/{Screeners,ScreenerDetail,Watchlists,Deployments}.tsx`, `frontend/src/routes/explainerContent.ts` — Shipped Screener/Watchlist UX clarity pass: scoped latest-version vs selected-run actions, readable schedule execution labels and weekday chips, readable criterion/result formatting, ResultsTable presets + `Decision reason`, template search/show-all, collapsed advanced run settings, Watchlist deep-link after save, and Deployment doctrine copy that entries come from Watchlists while exits come from Account-owned Positions scoped to the Deployment. Frontend-only; no trading spine/backend path changed.
- 2026-04-29 08:06:58 -04:00 · Codex · docs-added · `Operations_Turtle_Shell_Artifacts/SCREENER_WATCHLIST_UX_FIX_PLAN.md` — Added MAP fix plan plus UX/Nanyel/test-mapper final findings. Experts approved; remaining UX/test risks are non-blocking.
- 2026-04-29 08:06:58 -04:00 · Codex · lint · `frontend/src/routes/*Screener*.test.tsx`, `frontend/src/routes/{Watchlists,Deployments}.test.tsx`, `frontend/src/components/screener/*.test.tsx`, `frontend/scripts/headless-screener-watchlist.mjs` — Verification passed: focused frontend gate 24 passed; full frontend `npm.cmd test` 308 passed plus `lint:names` clean; `npm.cmd run typecheck` passed; `node --check scripts/headless-screener-watchlist.mjs` passed; `git diff --check` passed with CRLF warnings only. Leases released and Claude inbox notified.
- 2026-04-29 09:45:00 -04:00 · Claude · coordination · `backend/app/simulation/`, `backend/app/risk_resolver/`, `backend/tests/unit/simulation/`, `frontend/src/components/roadmap/researchRoadmap.ts` — Operator override: shipped "Short-side entries" research-roadmap slice across SimulatedPositionLedger (signed qty), SimulatedBroker (sell-to-open / buy-to-cover, side-aware protective + trailing logic), SimulatedTrade.side, RiskResolver (side-aware stop_distance), HistoricalReplayEngine (no-LONG-only short-circuit; same-side and opposite-side blocks). Backend unit suite 1374 passed (was 1369; +5 SHORT tests). Live broker path untouched. Roadmap entry flipped to shipped + cross-side flip filed as separate `planned`. Codex inbox notified; leases released.
- 2026-04-28 23:50:30 -04:00 · Codex · coordination · `frontend/src/api/`, `frontend/src/routes/`, `frontend/src/components/`, `backend/tests/unit/api/test_frontend_api_contract.py` — Operator override received: Codex now owns Alpaca-first Screener/Watchlist front-to-back for Step 9 UI/schema/contract work and release hardening; Claude notified, leases opened, backend doctrine remains locked.
- 2026-04-28 23:03:46 -04:00 · Codex · coordination · `backend/tests/unit/screener/test_screener_alpaca_first.py` — Added Step 10 backend release-gate flow: Alpaca Day Gainers -> edit fractionable ScreenerVersion -> run/rerun/diff -> static Watchlist -> dynamic Watchlist refresh snapshots -> Deployment entry universe -> exit rule requires PositionContext; full backend unit suite now `1369 passed, 6 warnings`. Frontend Step 9 remains pending Claude ack.
- 2026-04-28 22:52:37 -04:00 · Codex · route-added · `GET /api/v1/screeners/fields`, `GET /api/v1/screeners/templates`, `POST /api/v1/screeners/from-template`, `POST /api/v1/screeners/ai/interpret`, `GET /api/v1/market-lists`, `POST /api/v1/market-lists/{template_key}/run`, `POST /api/v1/screeners/runs/{run_id}/rerun`, `GET /api/v1/screeners/runs/{run_id}/diff`, `POST /api/v1/screeners/{screener_id}/archive`, `POST /api/v1/watchlists/{watchlist_id}/refresh`, `POST /api/v1/watchlists/{watchlist_id}/archive` — Alpaca-first Screener/Watchlist backend contract landed: typed fields, templates/market lists, advisory AI compile, rerun/compare, archive/delete guards, and dynamic Watchlist refresh snapshots.
- 2026-04-28 22:52:37 -04:00 · Codex · schema-added · `backend.app.screener` + `backend.app.watchlists` — `ScreenerExpression` AST, broker capability fields (`broker.tradable/fractionable/shortable/easy_to_borrow/active/exchange/asset_class/name`), run evidence/freshness/audit fields, dynamic Watchlist source rules, snapshot diff/evidence/source lineage; Alpaca provider adapters are bounded to Screener/Data provider surfaces.
- 2026-04-28 22:52:37 -04:00 · Codex · route-changed · `POST /api/v1/screeners/runs/{run_id}/save-as-watchlist`, `POST /api/v1/screeners/{screener_id}/delete`, `POST /api/v1/watchlists/{watchlist_id}/delete` — save-as-watchlist can create static or dynamic entry Watchlists, Screener delete now blocks when run history exists, and Watchlist delete blocks when snapshots or active Deployment references exist; archive is the safe history-preserving path.
- 2026-04-28 22:52:37 -04:00 · Codex · coordination · `backend/tests/unit` — Alpaca-first Screener/Watchlist backend slice verified green: full backend unit suite `1368 passed, 6 warnings`; frontend/shared UI implementation is pending Claude ack for the 22:24 inbox lease request.
- 2026-04-28 23:45:00 -04:00 · Claude · slice-shipped · **Slice 6d — Coherence validator + section severity bars — SHIPPED GREEN** (frontend + backend tests — `frontend/src/components/strategy_builder/editor/coherenceValidator.ts` (new: `CoherenceWarning`, `SectionId`, `validateCoherence()` implementing all 15 §9.1 rules: `htf_in_exit_not_entry`, `logical_exit_fires_immediately_after_entry`, `stop_target_ratio_nonsensical`, `intraday_no_time_exit`, `feature_not_supported_for_timeframe`, `feature_not_executable`, `stop_pct_out_of_range`, `target_pct_out_of_range`, `targets_runner_exceed_100`, `targets_below_100_no_runner_no_stop`, `short_enabled_no_short_entry`, `both_dir_asymmetric_exits`, `htf_confirmation_required_but_no_htf_feature`, `time_based_exit_no_unit`, `multi_target_slices_below_100`); `editor/coherenceValidator.test.ts` (new: 38 tests, 2+ per rule + dismissal suite); `editor/EditorPage.tsx` (adds `dismissedWarnings` state + `handleDismiss` callback, `coherenceWarnings` useMemo, `warningsFor()` helper, `canSave` gates on `coherenceErrors.length === 0`, save-bar hint shows first 3 client error messages, per-section warning routing to 9 sections); `editor/sections/CoherenceWarningsPanel.tsx` (rewrite: full implementation — backend errors block, client errors with no dismiss, warn/info with dismiss buttons, all-clear row when clean); `RequiredFeaturesSection`, `EntryRulesSection`, `StopPlanSection`, `TargetPlanSection`, `RunnerPlanSection`, `LogicalExitSection`, `TimeBasedExitSection`, `StrategyControlsSection`, `ExecutionPresetSection` each gain `warnings?: CoherenceWarning[]` prop + inline warning list + computed severity bar passed to `SectionCard`; `backend/tests/unit/strategy_composer/test_strategy_composer_service.py` (+3 tests: `test_save_draft_blocks_on_short_enabled_no_short_entry`, `test_save_draft_blocks_on_htf_confirmation_no_htf_feature`, `test_save_draft_passes_when_htf_feature_present` — backend defense-in-depth was already implemented in `_check_strategy_controls_coherence`, tests confirm it). Frontend gates: typecheck clean, vitest **219/219** (was 181/181, +38 new), lint:names clean. Backend: `pytest backend/tests/unit/strategy_composer -q` → 53 passed. Plan: `C:\Users\potij\.claude\plans\prefilled-editor-twin-river.md` §9 / §12 / §13. Resume Slice 6e (Curated starter catalog completeness pass, Sonnet 4.6).
- 2026-04-28 22:12:29 -04:00 · Codex · planning-added · `Operations_Turtle_Shell_Artifacts/ALPACA_FIRST_SCREENER_WATCHLIST_PLAN.md` — Created operator-requested 10-step Alpaca-first Screener/Watchlist execution plan with templates, market lists, Alpaca provider pack, AI advisory composition, run/rerun/compare, archive/delete, dynamic Watchlist refresh, auditability, three user reviewers, Alpaca Expert, Enemy Agent, and a new-window restart prompt. No implementation started; frontend/shared work requires Claude coordination before edits.
- 2026-04-28 22:52:00 -04:00 · Claude · slice-shipped · **Slice 6c — Page-2 prefilled editor — SHIPPED GREEN** (frontend only — `frontend/src/components/strategy_builder/FeatureIndex.tsx` rewrite (drawer + `onInsert` event API; PORTFOLIO scope filtered out at the Strategy-author surface), `frontend/src/components/strategy_builder/FeatureIndex.test.tsx` rewrite (drawer-surface assertions); new `frontend/src/components/strategy_builder/editor/` module: `editorState.ts` + `editorState.test.ts` (EditorState shape, preset↔draft sync, exit-rule bucketing, feature-ref collection) + `EditorPage.tsx` (host with sticky right-rail TOC + sticky save bar) + 13 section files under `sections/` (`SectionCard.tsx`, `SummarySection.tsx`, `RequiredFeaturesSection.tsx`, `EntryRulesSection.tsx`, `EntryPlanSection.tsx`, `StopPlanSection.tsx`, `TargetPlanSection.tsx`, `RunnerPlanSection.tsx`, `LogicalExitSection.tsx`, `TimeBasedExitSection.tsx`, `StrategyControlsSection.tsx`, `ExecutionPresetSection.tsx`, `CoherenceWarningsPanel.tsx`, `ResearchActionsBar.tsx`); `frontend/src/components/strategy_builder/LivePreviewPane.tsx` deleted; `frontend/src/routes/StrategyCompose.tsx` rewrite (Page-2 mounts `EditorPage`; Save → POST `/composer/drafts` → navigate `/strategies/:id` with `composerSavedToast` location.state for the Verify-in-Backtest banner; Regenerate confirm guard; SkippedWizardLanding when no draft); `frontend/src/routes/StrategyCompose.test.tsx` rewrite (full Page-1 → Page-2 happy path, save body capture, Summary edit-then-save invariant). 14 fixed sections each map 1:1 to a SignalPlan leg or a Strategy Controls field per Decision 5/9. FeatureIndex drawer is the only feature-insertion surface — no left rail. Stop / Target / Runner sections edit slices of the same `ExecutionStylePresetValue`; Execution preset section (#12) holds the canonical preset row. Time-based exit section bucket-renders exit_rules whose `logical_exit_rule.kind` is one of {bars_since_entry, time_in_position_seconds, time_of_day_et, minutes_before_session_close, session_window} per `feedback_logical_exit_is_the_only_exit_intent.md`. Coherence panel ships in stub form (backend-validation surface only; full client-side rule list ships in 6d). Research-actions bar disabled until save. Banned-name `Account Governor` flagged by lint — replaced with canonical `Governor` (matches `docs/architecture/NAMING_CONTRACT.md`). Plan: `C:\Users\potij\.claude\plans\prefilled-editor-twin-river.md` §0.1.1 / §5 / §6 / §11.2. Frontend gates: typecheck clean, vitest **181/181** (was 170/170, +11 new), lint:names clean. Resume Slice 6d (Coherence validator + section severity bars + backend defense-in-depth, Sonnet 4.6).
- 2026-04-28 19:28:00 -04:00 · Claude · slice-shipped · **Slice 5 — Cleanup — SHIPPED GREEN** (`backend/app/features/batch.py` deleted; `backend/app/features/incremental.py`, `backend/app/features/__init__.py`, `backend/app/chart_lab/preview_service.py`, `backend/app/simulation/historical_replay.py`, `backend/tests/smoke/test_paper_runtime_smoke.py`, `backend/tests/unit/pipeline/test_runtime_orchestrator.py`, `backend/tests/unit/simulation/test_historical_replay_engine.py`, `backend/tests/unit/chart_lab/test_chart_lab_preview_service.py`, `backend/tests/unit/features/test_incremental_feature_engine.py`, `backend/tests/unit/features/test_feature_engine_compute.py` (renamed from `test_batch_feature_engine.py`)). Strategies-Redesign closes here. `BatchFeatureEngine` + `BatchFeatureEngineError` deleted; the bulk-replay helper is now `IncrementalFeatureEngine.compute(plan, bars) -> FeatureFrameSet`, which drives `update()` per (symbol, timeframe) group. All callers (Chart Lab preview, historical replay, smoke + unit tests) migrated. The now-self-referential parity test in the renamed test file is reframed as `update()`-vs-`compute()` parity inside the canonical engine. Negative-assertion guards `"BatchFeatureEngine" not in source/dict` left as trivially-true tripwires that prevent reintroducing the symbol. Slice-1's deletion of `_symbols_from_request` / `suggested_universe.symbols` already covered the symbol-helper portion of the cleanup; the remaining negative-assertion tests stay. Memory: deleted `project_slice5_batch_engine_shim_removal.md`; added `reference_canonical_feature_engine.md`; updated `MEMORY.md` index. **Verification:** backend `pytest backend/tests/unit -q` → 1307/1307 passed; frontend `npm run typecheck` clean, `npm test` → 110/110 passed, `lint:names` clean. Pre-existing smoke failures (`MockAlpacaClient.get_orders(filter=...)`) reproduce on master and are unrelated to this slice. **Strategies-Redesign plan-file `memoized-discovering-creek.md` is fully shipped.**
- 2026-04-28 19:11:00 -04:00 · Claude · slice-shipped · **Slice 4 — Strategies list + detail polish — SHIPPED GREEN** (`frontend/src/routes/Strategies.tsx`, `frontend/src/routes/Strategies.test.tsx`) — Per locked plan `memoized-discovering-creek.md:340-348`: Strategies list header CTA hierarchy flipped — primary is now `Compose new strategy` (Sparkles, → `/strategies/compose` focused-mode route), blank-flow demoted to secondary `New blank strategy` (Plus, opens existing CreateStrategyDrawer). Empty state mirrors the dual-CTA hierarchy with updated copy naming Compose as the recommended path. `StrategyDetail.tsx` left untouched — its AI Composer header button already routes to the focused-mode composer via Slice 3's router move (no-op for Slice 4 per MAP minimal-precise-changes; the listed file was already satisfied). Two new vitest cases: primary CTA is wrapped by `<Link to="/strategies/compose">`; empty state surfaces both CTA labels (header + empty = 2× each). No backend contract change. Frontend gates green: 110/110 vitest (was 108/108), typecheck clean, lint:names clean. Resume plan at Slice 5 (Cleanup — delete `BatchFeatureEngine` shim, `_symbols_from_request`, `suggested_universe.symbols` helpers, memory updates).
- 2026-04-28 18:58:26 -04:00 · Codex · route-changed · `GET /api/v1/watchlists` — Fixed local 500 from legacy SQLite `watchlists(id,type,config_json,latest_symbols_json,...)` tables by migrating them to the canonical payload-backed Watchlist schema during repository initialization; response shape unchanged. Verification: route now returns 200 against `data/runtime.db`; `python -m pytest backend/tests/unit/watchlists backend/tests/unit/api/test_frontend_api_contract.py -q` → 17 passed; `npx vitest run src/routes/Watchlists.test.tsx` → 3 passed.
- 2026-04-28 21:55:00 -04:00 · Claude · slice-shipped · **Slice 1 — Backend contract reshape — SHIPPED GREEN** (`backend/app/strategy_composer/service.py`, `backend/app/domain/strategy_draft.py`, `backend/app/domain/execution_style.py`, `backend/app/strategy_composer/presets.py`, `backend/tests/unit/strategy_composer/test_strategy_composer_service.py`, `backend/tests/unit/api/test_strategy_composer_api.py`) — `AIComposerRequest` now requires `prompt` + optional `timeframe`/`initial_capital`/`feature_refs`/`execution_style_preset`/`execution_style_overrides`; `symbols` and `notes` are **extra-forbidden** (422 on old callers). `StrategyDraft` dropped `suggested_risk_plan` and `suggested_universe`; `suggested_execution_style` renamed to `execution_style`; added `signal_plan_shape: dict | None` (Tier-1 no-AI preview). `StrategyDraftComponentSnapshots` dropped `risk_plan` + `universe` fields. `_symbols_from_request`, `_default_risk_plan`, `_default_execution_style` helpers deleted. Five `ExecutionStylePresetKind` enum members + typed payload union + `build_preset_spec` / `build_execution_style_version` / `build_signal_plan_shape_preview` registry in `presets.py` all live. Tests: 37 composer-specific pass; full suite 1302/1302 passed. Frontend typed client (`frontend/src/api/schemas/strategyComposer.ts` + `frontend/src/api/strategyComposer.ts`) still sends the old body with `symbols` — will be updated in Slice 3 when the Compose UI is rebuilt. Until then the old frontend hits 422 on `/composer/preview` if it sends `symbols`, which is expected and acceptable (Compose page is being rebuilt in Slice 3). Resume plan at Slice 2 (Feature engine unification).
- 2026-04-28 14:35:00 -04:00 · Claude · coordination · `C:\Users\potij\.claude\plans\memoized-discovering-creek.md` — **Strategies redesign LOCKED** by operator (2026-04-28). Plan file is the durable handoff if context dies; resume from Slice 1 unless plan says otherwise. **Codex NOT consulted on this slice (operator directive).** Spine doctrine clarification (now memory `feedback_strategy_symbol_agnostic_spine.md`): Strategy is symbol-agnostic; Watchlists provide symbols; Deployments evaluate Strategy logic over Watchlist symbols; SignalPlans are symbol-specific and must include the concrete symbol; Accounts decide whether to act on each SignalPlan. **7 locked decisions:** (1) `IncrementalFeatureEngine` is canonical; `BatchFeatureEngine` becomes a temporary shim then is removed after parity tests pass — no parallel engines forever; (2) Five execution presets in fixed order — `Market Entry / Market Exit`, `Stop Entry / Market Exit`, `Bracket: Stop + Target`, `Bracket + Runner`, `Multi-Target Scale-Out` — each maps to a strict SignalPlan shape (deterministic builder, not a UI label); (3) Esc-on-dirty uses confirm dialog, no silent loss; (4) `Verify in Backtest` deep-link kept, launcher gets pre-bound query params; (5) "New blank strategy" kept but demoted, "Compose new strategy" is primary; (6) old `/strategies/:id/builder/new` route preserved; (7) **two-tier preview model** — Tier 1 always-live local validation (feature/preset/timeframe/SignalPlan-shape preview, no AI call), Tier 2 explicit `Generate with AI` button (no auto-AI, no background AI calls while typing). **5-slice plan** sequenced: S1 Backend contract reshape (drop symbols/notes from `AIComposerRequest`, add `feature_refs` + `execution_style_preset` + `execution_style_overrides`; drop `suggested_risk_plan`/`suggested_universe` from `StrategyDraft`, rename `suggested_execution_style` → `execution_style`; add `ExecutionStylePresetKind` enum + 5-preset → SignalPlan registry); S2 Feature engine unification (incremental canonical, batch shim, add rsi/atr/vwap, drop `batch_executable` flag, parity test); S3 Frontend full-screen Compose redesign (focused-mode route outside `AppShell`, FeaturePicker rewrite as left-rail searchable index with per-feature timeframe pills, `ExecutionStylePresetRow`, `LivePreviewPane` with Tier 1 + Tier 2); S4 Strategies list + detail polish; S5 Cleanup (delete legacy `BatchFeatureEngine`, `_symbols_from_request`, `suggested_universe.symbols` path). **Beginning Slice 1 next.**
- 2026-04-28 14:00:00 -04:00 · Claude · frontend-consumed · `frontend/src/api/providers.ts`, `frontend/src/routes/Providers.tsx`, `frontend/src/routes/Providers.test.tsx`, `backend/tests/unit/api/test_frontend_api_contract.py` — Picked up Codex's 13:01 `POST /api/v1/market-data/services/{service_id}/enable`. `MarketDataProvidersApi.enable(id)` posts the new route; disabled Market Data Provider cards now render an Enable secondary button next to the muted "Disabled" badge (mirrors the existing Disable affordance on enabled cards). Re-uses the existing `MarketDataServiceRecordSchema` (passthrough), which already typed `disabled_at`/`status=disabled`. New vitest case asserts a disabled card surfaces both Enable button and Disabled badge. Contract test registers the new POST route. Verification: `npx vitest run src/routes/Providers.test.tsx` → 4 passed (was 3); full `npm test` → 94 passed (was 93) + banned-name lint clean; `npm run typecheck` clean; `python -m pytest backend/tests/unit/api/test_frontend_api_contract.py backend/tests/unit/lint -q` → 221 passed.
- 2026-04-28 13:01:27 -04:00 · Codex · route-added · `POST /api/v1/market-data/services/{service_id}/enable` — Market Data services can now be re-enabled after operator disable. Validated services return to `status=valid`; unvalidated/invalid services return to `status=draft`; `disabled_at` is cleared and credentials/default tags are not rewritten. Verification: backend unit suite 1288 passed; frontend typecheck + 93 vitest/name-lint passed.
- 2026-04-28 13:01:27 -04:00 · Codex · route-changed · `backend/app/data_center/ingest_service.py::HistoricalBarIngestService.ensure_bars` — Backtest/Data Center cache gap handling now fetches only missing historical windows, merges with the existing dataset, and dedupes inclusive boundary bars by `(symbol,timeframe,timestamp)`. Exact cache hits still make zero provider calls; seeded run-window cache no longer redownloads the same visible bars just because Backtest requests warmup. Verification: backend unit suite 1288 passed; frontend typecheck + 93 vitest/name-lint passed.
- 2026-04-28 12:10:00 -04:00 · Claude · coordination · `backend/app/strategy_composer/service.py::AIComposerRequest` — Surgical cross-boundary fix (PROTOCOL §Decision Authority): added `notes: str | None = None` to `AIComposerRequest` and threaded it into the composed prompt + draft `description` so the operator-supplied "Notes for the AI" field actually flows through. Operator hit `extra_forbidden body.notes` 422 on every Generate-draft because the frontend has been sending a `notes` field since the slice landed but the backend `extra="forbid"` model didn't accept it. ~5-line diff inside the existing Pydantic model + compose() prompt-construction; no routes, schemas envelopes, or migrations changed. Composer tests `python -m pytest backend/tests/unit/strategy_composer backend/tests/unit/api/test_strategy_composer_api.py -q` → 24 passed. Heads-up filed in INBOX_CODEX.md inviting Codex to wire `notes` into the future AI provider call site.
- 2026-04-28 12:08:19 -04:00 · Codex · route-changed · `GET /api/v1/operations/overview` — Overview `deployments[]` now counts real Deployment records, persisted runtime deployment states, and current in-memory deployment contexts; historical internal orders and recent pipeline/runtime events no longer create phantom Dashboard deployments. Account/deployment detail can still project order lineage. Verification: `python -m pytest backend/tests/unit/api/test_server_startup.py backend/tests/unit/operations/test_operations_center_service.py backend/tests/unit/api/test_operations_routes.py -q` -> 38 passed; full backend unit run timed out before reporting.
- 2026-04-28 11:53:54 -04:00 · Codex · frontend-consumed · `frontend/src/routes/Dashboard.tsx`, `frontend/src/routes/Dashboard.test.tsx` — Dashboard now consumes existing `GET /api/v1/operations/overview` alongside `/api/v1/system/streams`. The Deployments KPI renders live deployment count with running/blocked context, and Open Positions renders `open_positions_count` with `open_orders_count` as sublabel; overview failures show `unavailable` instead of false zeroes. No backend route/schema change. Verification: `npx vitest run src/routes/Dashboard.test.tsx` -> 4 passed; `npm run typecheck` clean; banned-name lint clean.
- 2026-04-28 03:40:00 -04:00 · Claude · route-added · 12 new routes under `/api/v1/screeners` (list/create/get/patch/delete/add-version/run/list-runs/get-run/save-as-watchlist/presets/metrics) + `backend/app/screener/` (NEW module: `__init__.py`, `domain.py`, `store.py`, `sources.py`, `service.py`, `presets.py`, `runtime.py`) + `backend/app/api/routes/screener.py` (NEW) + `backend/app/api/server.py` (mount) + `backend/tests/unit/screener/{test_screener_service.py,test_screener_routes.py}` (18 tests) + `backend/tests/unit/api/test_frontend_api_contract.py` (12 new contract entries) + `frontend/src/api/{screener,schemas/screener}.ts` (NEW) + `frontend/src/components/screener/{Sparkline,CriteriaEditor,UniverseSourcePicker,ResultsTable}.tsx` (NEW) + `frontend/src/routes/{Screeners,ScreenerDetail}.tsx` (NEW) + `frontend/src/router.tsx` + `frontend/src/components/layout/SideNav.tsx` (Screeners entry under Author group) — **Screener slice (operator-commissioned solo build, FE + BE)**. Per operator directive 2026-04-28 ~01:50 ("build this yourself, no need to wait for Codex but let him know"), Codex notified via INBOX_CODEX heads-up before backend lease. Domain: typed `Screener` + `ScreenerVersion` + `ScreenerCriterion` (9 metric kinds: price / avg_volume_20d / relative_volume / gap_pct / change_pct / rsi_14 / atr_14_pct / prior_day_close / prior_day_range_pct; 6 operators: gte/gt/lte/lt/eq/between) + `ScreenerUniverseSource` (3 kinds: explicit symbol list, watchlist by id, built-in preset) + `ScreenerRun` + `ScreenerResultRow`. SQLite store lives in `backend/app/screener/store.py` with its own `screeners` / `screener_versions` / `screener_runs` tables, scoped per-call connection so it does not collide with Codex-owned `runtime_store.py` schema. Sources: `UniverseResolver` resolves explicit/preset/watchlist; `MetricSource` computes all 9 metrics from the existing `HistoricalBarIngestService` cache (Alpaca with the credential-injected client Codex shipped at 01:48; Yahoo as fallback per-symbol). Presets shipped: `liquid_large_caps` (43 symbols), `high_volume_etfs` (22), `magnificent_seven`. ScreenerRun is immutable — re-running spawns a new id; `cache_hit_rate` + `sources_used` recorded per run for operator visibility. `save-as-watchlist` POSTs through the existing `WatchlistService.create_watchlist` (NEVER mutates an existing Watchlist; doctrine guard). Frontend: `Sparkline` lead column on `ResultsTable` per chart-first doctrine; pill-row `CriteriaEditor`; name-first `UniverseSourcePicker` (no UUID paste — operators pick a Watchlist by name from the live `/api/v1/watchlists` list). New SideNav entry "Screeners" under the Author group; routes `/screeners` and `/screeners/:screenerId`. Verification: backend 18 unit tests + 6 contract additions = 26 pass (210s); frontend typecheck clean; vitest 92 pass (was 85); banned-name lint clean. Doctrine: Screener never deploys, attaches an Account, submits broker orders, or claims live readiness — discovery surface only.
- 2026-04-28 02:25:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/strategy_builder/` (new module: 8 files), `frontend/src/routes/StrategyCompose.tsx` (new), `frontend/src/routes/StrategyDetail.tsx` (rewrite), `frontend/src/routes/Strategies.tsx` (header), `frontend/src/router.tsx`, `frontend/src/api/{strategyComposer,schemas/strategyComposer}.ts` (new), `frontend/src/api/schemas/strategies.ts` (SignalRule.condition relaxed to optional + logical_exit_rule allowed), `frontend/src/components/roadmap/researchRoadmap.ts` (2 entries flipped → shipped) — **Strategy Builder + AI Composer frontend slice**: typed BuilderPane (visual condition pill builder + recursive ALL/ANY groups + LogicalExitRulePicker honoring all 7 LogicalExitRule kinds — feature_condition, bars_since_entry, time_in_position_seconds, time_of_day_et, minutes_before_session_close, session_window, hybrid; hybrid recursively composes child rules with operator=all|any) replaces the JSON textareas in StrategyDetail's Add/Edit Version drawers. New `/strategies/compose` page wires prompt + symbols + timeframe + initial_capital + notes through `POST /api/v1/strategies/composer/preview` → editable BuilderPane → `POST /api/v1/strategies/composer/drafts` → `launch_plans` block. Right-rail: live ValidationStatusCard + FeaturePlanPreviewCard + SimilarPatternsCard fed by `/strategies/builder/{features,features/aliases,features/validate,features/plan-preview,reuse-matches}` (debounced + cached via tanstack-query). LaunchPlansCard surfaces `chart_lab` / `backtest` / `walk_forward` actions, honoring `ready` + `missing_fields` (Backtest disabled until risk_plan_version_id + start + end; WF disabled until start + end; Chart Lab is always ready). Same card mounts on StrategyDetail using a locally-constructed launch_plans for any saved StrategyVersion. Doctrine: composer NEVER deploys, attaches an Account, submits a broker order, or claims live readiness — drafts only; backend `/composer/preview` consults the configured AI provider with deterministic FeatureRegistry-vocabulary validation. Tests: 71 → 85 (+14 new across `conditionUtils.test.ts`, `LogicalExitRulePicker.test.tsx`, `StrategyCompose.test.tsx`); typecheck clean; banned-name lint clean. Roadmap: "Strategy authoring UI for logical exit rules" → shipped, plus a new "AI Strategy Composer (prompt → validated draft → save → launch)" entry → shipped on Backtests roadmap. Heads-up: `frontend/src/api/schemas/strategies.ts::SignalRuleSchema` had `condition` as required; relaxed to optional + added `logical_exit_rule` (matches the existing backend SignalRule shape since the LogicalExitRule slice).
- 2026-04-28 02:18:00 -04:00 - Codex - route-added - `GET /api/v1/operations/evaluations` - Operations Account decisions timeline now returns `{evaluations: AccountSignalPlanEvaluation[]}` from durable SignalPlan-origin internal orders with `account_evaluation_id`, plus optional governor-decision trace; supports `account_id`, `deployment_id`, `signal_plan_id`, and `limit` filters. Backend unit suite 1255/1255 passing.
- 2026-04-28 01:48:00 -04:00 · Codex · route-changed · `backend/app/strategy_composer/service.py`, `backend/app/features/{parser,planner}.py`, `backend/app/data_center/ingest_service.py`, `backend/app/api/routes/{research_runs,research_jobs,data_center}.py` — Closed Claude's 01:25 operator-test gaps: Strategy Composer now persists normalized feature refs, FeaturePlanner defaults legacy bare bar refs to StrategyControls timeframe for existing drafts, and Alpaca historical research/data-center routes now inject an Alpaca StockHistoricalDataClient from a validated saved Alpaca Account's encrypted credentials. Backend unit suite 1252/1252 passing.
- 2026-04-28 01:25:00 -04:00 · Claude · coordination · `frontend/src/components/jobs/JobToaster.tsx`, `COORDINATION/INBOX_CODEX.md` — Operator hit toast spam on page reload (4 stale "Backtest failed" / "Optimization failed" toasts re-firing on every refresh) — JobToaster's seed-on-init flipped `initialized.current = true` against `list.data === undefined` (the first effect run before the useQuery resolved), so when real data landed every historical terminal job fired as a "new" toast. Fixed by gating on `list.data === undefined` so the seed runs against actual data. Plus filed Codex inbox request for two gaps the now-running backtest cascade exposed: (1) AI/composer-saved strategies use bare `close` / `open` feature refs which `parse_feature_expression` rejects with `FeaturePlanError: invalid feature expression syntax 'close'` — needs composer-side normalize to `<timeframe>.<kind>` or parser-side default-timeframe support; (2) `AlpacaBarsSource()` is instantiated with no client across all four research routes → `RuntimeError: AlpacaBarsSource requires an injected client` on every Alpaca-source backtest. Operator has valid Alpaca paper creds under `broker_accounts` but no path threads them into the historical-data client. Both filed; YahooBarsSource path is unblocked for well-formed strategies.
- 2026-04-28 01:05:00 -04:00 · Claude · frontend-consumed · `frontend/src/api/{schemas/,}/riskPlans.ts` — Picked up Codex's 00:58 list+detail enrichment. `RiskPlanDetailEnvelopeSchema` now declares the new top-level enrichment fields (`active_version_id`, `active_version`, `linked_accounts`, `backtest_usage`, `decision_stats`); `flattenRiskPlanDetail` adapter pulls them through instead of clobbering with empty defaults. Deleted the now-obsolete `enrichListWithActiveVersions` helper and its `.then(...)` wrap on `RiskPlansApi.list()` since list rows arrive enriched natively. Frontend typecheck clean; 71/71 vitest still green. AccountRiskPlanCard's picker is now fully wired end-to-end (list → version_id → PUT) without the parallel-detail-fetch fallback. Net delete on the helper offsets the small adapter add; the §9.3 detail tabs (Account Assignments / Backtest Usage / Decision Cards) light up automatically once the operator has actual decision-card / backtest data.
- 2026-04-28 00:58:00 -04:00 · Codex · route-changed · `GET /api/v1/risk-plans`, `GET /api/v1/risk-plans/{risk_plan_id}` — Risk Plan list rows now include `active_version_id`, `active_version`, `linked_account_count`, and `last_used_at`; detail envelopes now include `active_version_id`, `active_version`, `linked_accounts`, `backtest_usage`, and `decision_stats`. Backend unit suite 1249/1249 passing.
- 2026-04-28 00:55:00 -04:00 · Claude · coordination · `backend/app/data_center/ingest_service.py::YahooBarsSource`, `backend/tests/unit/api/test_data_center_routes.py`, `frontend/src/routes/SimLab.tsx` — Surgical cross-boundary fix (PROTOCOL §Decision Authority): `YahooBarsSource.fetch` flattens `pd.MultiIndex` columns before iterating rows. yfinance ≥ 0.2 returns a MultiIndex on `df.columns` even for single-ticker downloads, which made `row["Open"]` a Series and broke `float(...)` with `TypeError: float() argument must be ... not 'Series'`. Operator hit this end-to-end on Backtest after `pip install yfinance` brought yfinance 1.3.0 into the venv. 6-line diff inside `YahooBarsSource.fetch`; no routes, schemas, or migrations changed; heads-up filed in INBOX_CODEX.md inviting Codex to refactor into a module-level helper or pin a yfinance constraint if preferred. Plus 2 new regression guards (`test_yahoo_bars_source_flattens_multi_index_columns`, `test_yahoo_bars_source_handles_flat_single_level_columns`) in the data_center routes test file. Also separately tightened Sim Lab UX: bumped `bar_count` default from 12 to 500, added per-chart headers showing symbol / timeframe / window / bar+fill counts, rewrote the page banner from backend-plumbing to "What you're seeing" — operator confusion on whether the bars were sample data or real OHLC was the trigger.
- 2026-04-28 00:35:00 -04:00 · Claude · coordination · `backend/tests/unit/api/test_frontend_api_contract.py`, `frontend/src/api/riskPlans.ts`, `frontend/src/components/risk_plans/AccountRiskPlanCard.tsx`, `COORDINATION/INBOX_CODEX.md` — Post-slice operator-test follow-up across three concerns. (1) **Contract drift guard**: 6 new payload-shape tests in `test_frontend_api_contract.py` POST the exact frontend `CreateRiskPlanRequest`/`PatchRiskPlanRequest` shapes, lock the `{risk_plan, versions}` envelope on detail/create/patch, lock the AI-draft response keys (`risk_plan`, `risk_plan_version`, `warnings`, `ai_provider_id`, `ai_provider_name`, `boundary_guardrails`), and add a regression guard that asserts 422 + `extra_forbidden` on `body.ai_notes` so the bug fixed earlier today cannot recur silently. Backend unit suite: 1239 → 1245 (+6). (2) **AccountRiskPlanCard end-to-end fix**: the save mutation used to send the OLD `risk_plan_id` with the NEW `risk_plan_version_id` after a switch, which the backend rejects with `risk_plan_version_id does not belong to risk_plan_id`; now resolves the matching plan_id from the same list query the picker reads, gates Save on a fully-resolved selection, and surfaces the no-selection guard in the button-disabled state. Plus `RiskPlansApi.list()` now enriches list rows with `active_version` via parallel detail fetches (`enrichListWithActiveVersions`) — without that, the Picker disables every option because backend list rows lack `active_version_id`. End-to-end PUT `/api/v1/accounts/{id}/risk-plan` verified by curl: empty → assigned → re-read returns the assigned plan. Frontend typecheck clean; vitest 71/71 still green. (3) **Codex inbox request** filed for the proper backend-side fix: include `active_version_id`/`active_version`/`linked_account_count`/`last_used_at` on list rows, and `linked_accounts`/`backtest_usage`/`decision_stats` on the detail envelope (§9.2 + §9.3 enrichments). When that lands the frontend's `enrichListWithActiveVersions` early-out becomes a no-op and I delete the helper.
- 2026-04-28 00:18:00 -04:00 · Claude · coordination · `Operations_Risk_Plan_Slice/STATUS.md` + `COORDINATION/LEDGER.md` — Risk Plan slice loop closed by Claude after end-to-end verification. Re-ran the full termination gate from a clean shell: `python -m pytest backend/tests/unit -q` → 1239 passed (110.98s); `cd frontend && npm.cmd run typecheck` → clean; `cd frontend && npm.cmd test` → 71/71 passed; banned-name lint clean. Every checkbox in `Operations_Risk_Plan_Slice/STATUS.md` is `[x]` (B1..B8 + F1..F11 + 3 cross-cutting). Loop terminated per the doctrine line in STATUS.md.
- 2026-04-27 23:55:00 -04:00 · Codex · coordination · `Operations_Risk_Plan_Slice/STATUS.md` — Risk Plan slice complete — full contract shipped, no MVP. Backend unit suite 1239/1239 passing; focused backend acceptance/guardrail suite 44/44 passing; frontend typecheck clean; frontend tests 71/71 passing with banned-name lint clean.
- 2026-04-27 23:45:00 -04:00 · Codex · route-added · `POST /api/v1/walk-forward/runs/{run_id}/save-risk-plan`, `POST /api/v1/optimization/runs/{run_id}/save-risk-plan` — B7 save-as-draft RiskPlan endpoints landed; they create draft-only RiskPlans with `source=walk_forward_recommended` or `source=optimization_generated` from recommendation/winner parameters and do not activate or assign accounts. Backend unit suite 1239/1239 passing.
- 2026-04-27 23:30:00 -04:00 · Codex · schema-added · `backend/app/research/{backtests,walk_forward,optimization}` — B6 evidence persistence verified: Backtest/WF/Optimization evidence now carries real `risk_plan_id`/`risk_plan_version_id` where applicable, and persisted RiskDecisionCards are asserted against real RiskPlan rows; backend unit suite 1237/1237 passing.
- 2026-04-27 23:15:00 -04:00 · Codex · schema-added · `backend/app/research/risk_plan_lookup.py` + Backtest/WF/Optimization services — B5 fabrication paths removed: research services now require and load saved `RiskPlanVersion` rows, adapt them to the legacy `RiskProfileVersion` wire shape for RiskResolver, and tests create real RiskPlans; backend unit suite 1237/1237 passing.
- 2026-04-27 23:05:00 -04:00 · Codex · route-added · `POST /api/v1/risk-plans/ai-draft` — B4 AI-draft route landed: consults configured valid AI provider, returns unsaved `status=draft`, `source=ai_generated`, `ai_summary` populated, warnings and boundary guardrails included; never activates or assigns to an Account. Backend unit suite 1236/1236 passing.
- 2026-04-27 23:00:00 -04:00 · Claude · schema-added · `frontend/src/api/schemas/riskPlans.ts` + `frontend/src/api/riskPlans.ts` (new) — Full Risk Plan typed client per RISK_PLAN_SIGNALPLAN_BACKTEST_BACKEND_CONTRACT §4 + §8. Schemas: `RiskPlanStatus`, `RiskPlanTier`, `RiskPlanSource`, `RiskPlanVersionStatus`, `RiskPlanSizingMethod`, `WholeShareRounding`, `RiskPlanConfig` (~30 fields per §4.3), `RiskPlanVersion`, `RiskPlanAiNotes`, `RiskPlanLinkedAccount`, `RiskPlanBacktestUsage`, `RiskPlanDecisionStats`, `RiskPlanSummary`, `RiskPlanDetail`, `AccountRiskPlanAssignment`, `RiskPlanAiDraftRequest/Response`, `CreateRiskPlanRequest`, `PatchRiskPlanRequest`, `NewRiskPlanVersionRequest`, `PutAccountRiskPlanRequest`. All `.passthrough()` so additive backend fields never reject the typed client. `RiskPlansApi.{list,get,create,patch,listVersions,newVersion,activate,archive,aiDraft,getAccountAssignment,putAccountAssignment}`.
- 2026-04-27 23:00:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/risk_plans/` (new module) + `frontend/src/routes/{RiskPlans,RiskPlanDetail}.tsx` (new) + `frontend/src/router.tsx` + `frontend/src/components/layout/SideNav.tsx` — **Full Risk Plan slice frontend (F1..F11)**: list page with 5 filters (status / tier / source / score-min / score-max + search) + per-row actions (View / Edit Draft / Duplicate / Use-in-Backtest / Compare / Assign / Archive); 10-tab detail page (Overview / Sizing / Exposure Limits / Loss Limits / Position Rules / Account Assignments / Backtest Usage / Decision Cards / Versions / AI Notes); Create/Edit drawer with full RiskPlanConfig form (~30 fields per §4.3) + per-field validation feedback per §9.4 (missing-stop with risk_percent sizing, fractional+rounding conflict, aggressive-settings warning, oversized risk-per-trade / max-position / max-gross-exposure / max-daily-loss warnings); RiskPlanPicker with inline Risk Score / Tier / Sizing / Risk-per-trade / Max-position / Daily-loss / Max-DD / Open-positions card + "View Risk Plan" deep-link; CompareRiskPlansDialog with side-by-side field diff across 7 field groups + signed `(±N)` numeric deltas; SaveAsRiskPlanButton + recommendationPrefill helper used by WF RecommendedRiskPlanCard (`source=walk_forward_recommended`) and Optimization winner card (`source=optimization_generated`); AccountRiskPlanCard mounted in AccountDetailDrawer (per memory: credentials/settings inline on Account cards).
- 2026-04-27 23:00:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/backtests/RunBacktestDrawer.tsx` + `frontend/src/components/walkforward/RunWalkForwardDrawer.tsx` + `frontend/src/components/optimization/RunOptimizationDrawer.tsx` + `frontend/src/routes/Backtests.tsx` — UUID-paste fields removed; replaced with `<RiskPlanPicker>`. Backtest drawer: `risk_plan_version_id` is required (`canSubmit` gates on selection), drawer accepts `defaultRiskPlanVersionId` prop and the Backtests page reads `?risk_plan_version_id=` from the URL so "Use in Backtest" from the Risk Plan list deep-links straight into a pre-filled drawer. WF + Optimization drawers add a "Base Risk Plan" picker on the sweep grid (sweeps the selected field on top of this base config; recommended/winner can be saved as a draft Risk Plan via Save-as-Risk-Plan). All three drawers POST to the existing jobs endpoints — async dispatch unchanged.
- 2026-04-27 23:00:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/roadmap/researchRoadmap.ts` — Roadmap statuses flipped to `shipped`: "Risk Plan picker + product-facing Risk Plan model" (Backtests roadmap), "Save recommended Risk Plan as a real RiskPlanVersion" (WF roadmap), "Save winner as a draft Risk Plan version" (Optimization roadmap).
- 2026-04-27 23:00:00 -04:00 · Claude · lint · `frontend/src/routes/RiskPlans.test.tsx` (4 tests: empty / happy / status-filter / drawer-open), `frontend/src/components/risk_plans/RiskPlanDrawer.test.tsx` (2 tests: validation blocks save when name empty + AI draft loads but requires explicit operator save — doctrine assertion per non-negotiable §13), `frontend/src/components/risk_plans/RiskPlanPicker.test.tsx` (2 tests: emits version id + renders inline metadata). Frontend total: 63 → 71 (+8 new). `npm run typecheck` + 71/71 vitest + banned-name lint all clean.
- 2026-04-27 23:00:00 -04:00 · Claude · coordination · `backend/tests/unit/api/test_frontend_api_contract.py` — registered all 11 Risk Plan routes (`GET/POST /api/v1/risk-plans`, `GET/PATCH /api/v1/risk-plans/{risk_plan_id}`, `GET/POST /api/v1/risk-plans/{risk_plan_id}/versions`, `POST /api/v1/risk-plans/{risk_plan_id}/activate`, `POST /api/v1/risk-plans/{risk_plan_id}/archive`, `POST /api/v1/risk-plans/ai-draft`, `GET/PUT /api/v1/accounts/{account_id}/risk-plan`). Test passes for 10 routes Codex shipped under B3 and currently fails only on the unimplemented `POST /api/v1/risk-plans/ai-draft` (B4 deliverable). The failing assertion is the **handoff to Codex** — it goes green when B4 lands.
- 2026-04-27 22:50:00 -04:00 · Codex · route-added · `backend/app/api/routes/risk_plans.py` — B3 RiskPlan API routes registered: `GET/POST /api/v1/risk-plans`, `GET/PATCH /api/v1/risk-plans/{risk_plan_id}`, `POST/GET /api/v1/risk-plans/{risk_plan_id}/versions`, `POST /api/v1/risk-plans/{risk_plan_id}/activate`, `POST /api/v1/risk-plans/{risk_plan_id}/archive`, `GET/PUT /api/v1/accounts/{account_id}/risk-plan`; backend unit suite 1234/1234 passing.
- 2026-04-27 22:05:12 -04:00 · Codex · schema-added · `backend/app/domain/strategy_draft.py` + `backend/app/strategy_composer/service.py` + `backend/app/strategy_composer/__init__.py` — `StrategyDraft` now includes additive `launch_plans` for Chart Lab (`GET /api/v1/chart-lab/stream`), Backtest async job (`POST /api/v1/research/jobs/backtest`), and Walk-Forward async job (`POST /api/v1/research/jobs/walk-forward`). Backtest launch is intentionally not ready until operator supplies `risk_plan_version_id`, `start`, and `end`; Walk-Forward launch is not ready until operator supplies `start` and `end`. `StrategyDraftLaunchPlan` and `StrategyDraftLaunchPlans` are exported from `backend.app.strategy_composer`.
- 2026-04-27 22:05:12 -04:00 · Codex · docs-added · `docs/system_rebuild_outputs/STRATEGY_BUILDER_FRONTEND_CONTRACT.md` — Claude handoff doc added with endpoint map, request/response schemas, validation behavior, examples, AI composer response, validation errors, recommended frontend flow, and known gaps/TODOs. Doctrine captured: Strategy Builder is draft-only; Strategy does not own Risk or Universe; no Deployment/Account/broker action from composer.
- 2026-04-27 22:05:12 -04:00 · Codex · lint · `backend/tests/unit/strategy_composer/test_strategy_composer_service.py` + `backend/tests/unit/api/test_strategy_composer_api.py` — Added contract coverage for all logical_exit rule kinds requested (`bars_since_entry`, `time_in_position_seconds`, `time_of_day_et`, `feature_condition`, `hybrid`), frontend-safe response cores, launch plan payload templates, and component snapshot launch plans. Targeted tests: 23 passed, 1 warning. Full backend unit suite: 1221 passed, 6 warnings.
- 2026-04-27 21:49:31 -04:00 · Codex · coordination · `backend/app/strategy_composer/`, `backend/app/api/routes/strategies.py`, `backend/tests/unit/strategy_composer/`, `backend/tests/unit/api/test_strategy_composer_api.py` — Strategy Builder / AI Composer review recursion complete with AI Architect, Quant Strategist, Fullstack, Alpaca Engineer, and Nanyel approval. Fixes: invalid AI prompt drafts cannot save; RSI is not valid for backtest until batch-executable; unsupported prompt terms mark NEEDS_OPERATOR instead of silent green-bar conversion; minutes-before-close parses as session-close exit; blank symbols fall back safely; indicator tokens are not inferred as symbols; execution default is broker-neutral; typed feature catalog/snapshots added; composer boundary guard blocks broker/order/deployment/runtime imports. Backend unit suite: 1214 passed, 6 warnings.
- 2026-04-27 23:00:00 -04:00 · Claude · schema-added · `backend/app/domain/research_job.py` (new) — `ResearchJob` + `ResearchJobStatus {queued, running, completed, failed, canceled}` + `ResearchJobKind {backtest, walk_forward, optimization}` + `ResearchJobProgress {current, total, label, message, updated_at}`. Persisted in new SQLite `research_jobs` table indexed by status/kind/result_run_id with `payload` JSON + structured columns for fast filtering.
- 2026-04-27 23:00:00 -04:00 · Claude · route-added · 6 routes: `POST /api/v1/research/jobs/{backtest|walk-forward|optimization}` (enqueue + return immediately), `GET /api/v1/research/jobs?status=&kind=&limit=`, `GET /api/v1/research/jobs/{job_id}`, `POST /api/v1/research/jobs/{job_id}/cancel`. Each POST takes `{request, operator_session_id?, metadata?}` where `request` is the same payload the existing sync POST routes accept.
- 2026-04-27 23:00:00 -04:00 · Claude · schema-added · `backend/app/research/jobs/` (new module: `runner.py`) + `backend/app/research/progress.py` (new) — `ResearchJobRunner` backed by ThreadPoolExecutor (max_workers=4), per-job thread-lock for race-free progress writes, `JobReporter` implementation of `ProgressReporter` that writes progress to SQLite + reads cancel toggle from SQLite. Cooperative cancellation: services check `reporter.cancel_requested()` between folds (WF) / candidates (Optimization) and raise `RunCanceled` to abort cleanly. Existing sync POST routes still work unchanged (services accept an optional `progress_reporter`; sync path passes None → NULL_REPORTER no-op).
- 2026-04-27 23:00:00 -04:00 · Claude · route-changed · `WalkForwardExecutionService.create_run` + `OptimizationExecutionService.create_run` accept an optional `progress_reporter: ProgressReporter | None` on the request dataclass. WF emits per-fold progress (`current=fold_index+1, total=fold_count, label="folds"`); Optimization emits per-candidate progress (`label="candidates"`). Both check `cancel_requested()` between iterations.
- 2026-04-27 23:00:00 -04:00 · Claude · frontend-consumed · `frontend/src/api/researchJobs.ts` + `frontend/src/api/schemas/researchJobs.ts` (new) — `ResearchJobsApi.{submitBacktest, submitWalkForward, submitOptimization, list, get, cancel}`. `frontend/src/components/jobs/JobMonitor.tsx` (new) — pulse-dot button in each research page header, expands to a popover listing active + recent jobs with progress bars, status badges, per-job cancel, link to result_run_id when complete. Polls every 2s when active jobs exist, every 15s when terminal. The 3 research drawers (Backtest / WF / Optimization) now POST to the jobs endpoints and return immediately — drawer closes, JobMonitor surfaces progress, list page auto-refreshes once the job completes.
- 2026-04-27 23:00:00 -04:00 · Claude · lint · `backend/tests/unit/research/test_research_job_runner.py` (4 tests: dispatch+complete with result_run_id, failure capture, cooperative cancel via threading, monotonic-progress visibility from a separate caller). Backend total: 1198 → 1206 (+4 from this slice; +4 from Codex's parallel strategy_composer landing). Frontend: 63/63 vitest + typecheck + banned-name lint clean. Contract test extended for the 6 new jobs routes. **Operator-visible payoff:** the Roadmap card's #1 item (Async job queue + per-fold/per-candidate progress + cancel) is now SHIPPED. Operators can navigate freely while runs execute in the background; cancel button works; per-fold WF + per-candidate Optimization progress visible in the JobMonitor.
- 2026-04-27 23:25:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/jobs/{JobToaster,ResearchJobsHubCard}.tsx` (new) + `frontend/src/components/layout/AppShell.tsx` + `frontend/src/routes/Dashboard.tsx` + `frontend/src/components/roadmap/researchRoadmap.ts` — completion-toast + Dashboard hub card finish the async-runs UX story. **JobToaster** mounted once at AppShell level polls `/api/v1/research/jobs` globally (2s when active jobs exist, 15s when terminal) and fires a Radix toast whenever any research job transitions to a terminal state — completed → ok-tone with "View results →" link to the right surface; failed → danger-tone with error preview + link; canceled → warn-tone confirmation. Initialization seeds the seen-terminal set with current state so historical jobs don't re-fire on page reload. **ResearchJobsHubCard** added to the Dashboard (after `ChartLabHubCard`) showing the latest 10 research runs across all kinds with progress bars, per-row Cancel, and click-through to the originating research surface — operators see recent runs without navigating to a research page first. Roadmap entry "Completion toast + Dashboard 'last 10 runs' card" flipped from planned → shipped on all three research roadmaps. Frontend typecheck + 63/63 vitest + banned-name lint all clean. **Run-lifecycle UX surface is now complete end-to-end.**
- 2026-04-27 21:28:11 -04:00 · Codex · route-added · `backend/app/api/routes/strategies.py` + `backend/app/strategy_composer/` + `backend/app/domain/strategy_draft.py` — Strategy Builder / AI Composer backend contracts added: feature catalog + aliases + validation + FeaturePlanPreview; condition parser for ConditionExpression + LogicalExitRule; reuse matching; composer preview; draft save. Save is draft-only: creates StrategyVersion draft, snapshots suggested Risk Plan / Execution Style / Universe / Backtest Plan, creates no Deployment, no broker action, and no live-readiness claim. Claude notified to reference only `file:///C:/Users/potij/OneDrive/AI_things/mockup_review.html` for UI direction. Backend unit suite: 1206 passed, 6 warnings.
- 2026-04-27 20:55:00 -04:00 · Claude · schema-added · `backend/app/research/walk_forward/` (new module: `window_planner.py`, `selector.py`, `recommendation.py`, `service.py`) — Walk-Forward end-to-end on the unified spine. Each fold runs `HistoricalReplayEngine` twice (IS + OOS) using the same SignalPlanBuilder + RiskResolver + RiskDecisionCard chain Backtest uses. `WalkForwardExecutionService` orchestrates fold sequence via rolling/anchored window planner; per-IS-fold scores every sweep candidate via `score_candidate(criterion=max_dd_bounded_sharpe)`; picks IS-best via `select_winner`; replays the winner OOS; aggregates across folds via `build_recommendation`. Mode = `walk_forward` on every emitted RiskDecisionCard.
- 2026-04-27 20:55:00 -04:00 · Claude · route-changed · `POST /api/v1/walk-forward/runs` — request schema extended (additive, passthrough): `symbols`, `start`, `end`, `timeframe`, `initial_capital`, `cost_model`, `source`, `adjustment_policy`, `window_mode {rolling|anchored}`, `is_length`, `oos_length`, `step`, `max_folds`, `selection_criterion {max_dd_bounded_sharpe (default) | sharpe | sortino | calmar | expectancy | hit_rate}`, `sweep {enabled, base_risk_plan_version_id?, parameters: [{field, values}]}`, `monte_carlo`, `fold_pass_threshold_sharpe`. When `symbols` is supplied the route drives the spine WF service; otherwise the legacy placeholder evidence-row path (window_count + passed_window_count + metrics) still works for tests that don't pass symbols.
- 2026-04-27 20:55:00 -04:00 · Claude · schema-added · `WalkForwardRun.metrics` JsonDict now carries (additive, passthrough): `selection_criterion`, `window_mode`, `fold_count`, `folds: [{fold_index, is_start, is_end, oos_start, oos_end, selected_parameters, is_metrics, oos_metrics}]`, `recommended_risk_plan {parameters, score, stability_metrics, drawdown_metrics, out_of_sample_metrics, explanation}`, `recommendation {ship_recommended | needs_more_data | do_not_ship}`, `thresholds_applied`, `candidates: [{parameters, oos_sharpe, oos_max_dd, oos_return, oos_hit_rate, stability, picked_in_folds, score, recommended}]`, `monte_carlo`, `risk_decision_card_ids`, plus aggregate metrics `{median_oos_sharpe, mean_oos_sharpe, oos_sharpe_p25, oos_sharpe_p75, median_oos_return, oos_max_dd, oos_hit_rate, is_oos_decay {sharpe}, parameter_stability_score, regime_fit_score, folds_passed_ratio, folds_passed_count}`. Recommendation defaults (operator-authorized 2026-04-27): score = `0.6 × oos_sharpe_p25 + 0.4 × stability`; ship thresholds = `oos_sharpe_p25 ≥ 0.5 ∧ is_oos_decay.sharpe < 0.5 ∧ folds_passed_ratio ≥ 0.6`; do-not-ship = `median_oos_sharpe < 0 ∨ is_oos_decay.sharpe > 1.5`. All thresholds tunable via `ship_thresholds` payload.
- 2026-04-27 20:55:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/WalkForward.tsx` + `frontend/src/components/walkforward/{RunWalkForwardDrawer,RecommendedRiskPlanCard,RiskPlanCandidateTable}.tsx` (new) + `frontend/src/api/schemas/researchRuns.ts` — Run-new-walk-forward drawer (Strategy + version + symbols + dates + timeframe + initial capital + cost model + source + window mode + IS/OOS/step lengths + max folds + selection criterion + collapsible sweep grid editor + Monte Carlo). Detail page now renders `RecommendedRiskPlanCard` (parameters, score, OOS metrics, stability, picked-in-folds, explanation, ship/no-ship tone-badge) and `RiskPlanCandidateTable` (full sweep landscape sortable by score with recommended row badged). Existing per-fold/decay/heatmap awaiting-API panels stay as scaffolds for follow-up slices. Frontend typecheck + 63/63 vitest + banned-name lint all green.
- 2026-04-27 20:55:00 -04:00 · Claude · lint · `backend/tests/unit/research/test_walk_forward_window_planner.py` (5 tests), `test_walk_forward_recommendation.py` (7 tests), `test_walk_forward_spine_integration.py` (1 end-to-end test that sweeps `fixed_shares ∈ {5, 10, 20}` across 3 folds and verifies aggregate metrics + recommendation + candidate landscape). Backend total: 1141 → 1159 (+18 new). All passing.
- 2026-04-27 21:05:00 -04:00 · Claude · route-changed · WF recommendation defaults updated per operator tweak (2026-04-27 21:00 message): score weights are now 50/50 (Sharpe / stability) by default — was 60/40. Ship gate adds `oos_max_dd ≥ -0.25` (drawdowns deeper than 25% block ship_recommended). do_not_ship gate adds `oos_max_dd ≤ -0.40` (catastrophic drawdowns force do_not_ship outright regardless of Sharpe). All weights + thresholds tunable per-run via `score_weights` + `ship_thresholds` payloads on the WF POST request; backend exports `DEFAULT_SCORE_WEIGHTS` + `DEFAULT_SHIP_THRESHOLDS` constants and surfaces them on every recommendation response (`default_score_weights` + `default_thresholds` keys) so operator UIs can offer a Reset.
- 2026-04-27 22:15:00 -04:00 · Claude · schema-added · `backend/app/research/optimization/` (new module: `grid_planner.py`, `landscape.py`, `service.py`) + `backend/app/research/window_runner.py` (shared per-window helper extracted from WF). Optimization runs a parameter sweep over one window via the unified spine — grid or random search, max_candidates default 200, GRID_HARD_LIMIT 1000 (operator protection: 6-parameter 5⁶=15,625 grids fail fast). Same `HistoricalReplayEngine` Backtest + WF use; `mode = optimization` on every emitted RiskDecisionCard. Per-candidate scored via the same `score_candidate(criterion=max_dd_bounded_sharpe)` selector WF uses. **Doctrine:** hypothesis generation, NOT ship-readiness. Output ALWAYS carries `needs_walk_forward_validation: true` + a pre-baked `follow_up_walk_forward_request` payload (top-K candidates feed the WF sweep grid; default K=3) so the UI can offer a one-click "Validate with Walk-Forward" handoff.
- 2026-04-27 22:15:00 -04:00 · Claude · route-changed · `POST /api/v1/optimization/runs` — request schema extended (additive, passthrough): `symbols`, `start`, `end`, `timeframe`, `initial_capital`, `cost_model`, `source`, `adjustment_policy`, `method {grid|random}`, `max_candidates` (default 200), `seed`, `selection_criterion {max_dd_bounded_sharpe (default) | sharpe | sortino | calmar | expectancy | hit_rate}`, `sweep {base_risk_plan_version_id?, parameters: [{field, values}]}`, `monte_carlo`, `runners_up_threshold_pct` (default 0.05), `walk_forward_handoff_top_k` (default 3), `heatmap_dimensions`. When `symbols` + `sweep` are supplied the route drives the spine optimization service; otherwise the legacy placeholder evidence-row path still works.
- 2026-04-27 22:15:00 -04:00 · Claude · schema-added · `OptimizationRun.best_metrics` JsonDict now carries (additive, passthrough): `method`, `selection_criterion`, `seed`, `max_candidates`, `candidate_count`, `candidates: [{candidate_index, parameters, metrics, score, trade_count, risk_decision_card_ids, recommended}]`, `landscape_summary {score_min/p25/p50/p75/p95/max, sharpe_min/max, max_dd_best/worst}`, `runners_up`, `heatmap {x_field, y_field, x_values, y_values, cells: 2D best-score-per-coord}`, `sweep_grid_shape`, `monte_carlo`, `needs_walk_forward_validation: true`, `follow_up_walk_forward_request` (pre-baked WF POST payload), `best_parameters`, `best_metrics`, `winner_score`. Heatmap auto-picks the two highest-variance dimensions when grid has 3+ params; falls back to first two when no discriminating dims.
- 2026-04-27 22:15:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/Optimization.tsx` (rewrite from `ResearchEvidencePage` stub) + `frontend/src/components/optimization/{RunOptimizationDrawer,HypothesisBanner,LandscapeHeatmap,CandidateTable}.tsx` (new) + `frontend/src/api/schemas/researchRuns.ts` — Run-new-optimization drawer (Strategy + version + symbols + dates + cost model + grid editor with add/remove parameter rows + method/grid-or-random + max_candidates + seed + selection criterion + Monte Carlo + advanced runners-up + WF handoff top-K). Detail page leads with `HypothesisBanner` (warning tone, "Validate with Walk-Forward →" button POSTs the pre-baked handoff payload to WF route + navigates to the new WF run). Then KPI cards (winner score / Sharpe / max-DD / candidate count), winner parameters, `LandscapeHeatmap` (2D colour-saturation grid for 2-param sweeps, auto-picks top-2-variance dimensions for 3+), and `CandidateTable` (full sweep landscape sortable by score, recommended row badged). `OptimizationRunRequestSchema` collapsed to one schema accepting both spine-driven + legacy placeholder shapes.
- 2026-04-27 22:15:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/roadmap/RoadmapCard.tsx` + `researchRoadmap.ts` (new) — shared `RoadmapCard` component (collapsible, status enum `shipped | in_design | planned | parked`, badge counts in header) added to all three research pages (Backtests, Walk-Forward, Optimization). Per-page roadmap content surfaces every item I named as "out of scope" in that slice's plan, plus shipped follow-ups. **Highest-priority item on every page**: Async job queue + per-fold/per-candidate progress + cancel + completion notifications — operator UX gap surfaced loudly so they know runs are synchronous today (closing the browser tab during a long run is unsafe; the persisted evidence still arrives via auto-refresh on the list page).
- 2026-04-27 22:15:00 -04:00 · Claude · lint · `backend/tests/unit/research/test_optimization_grid_planner.py` (9 tests), `test_optimization_landscape.py` (6 tests), `test_optimization_spine_integration.py` (2 tests including the GRID_HARD_LIMIT operator-protection assertion). Backend total: 1164 → 1186 (+22 new). Frontend: 63/63 vitest + typecheck + banned-name lint all clean. LOCKS released.
- 2026-04-27 21:05:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/walkforward/RunWalkForwardDrawer.tsx` — new "Recommendation tuning" section with Sharpe-weight + stability-weight inputs + ship-max-DD-floor + do_not_ship-max-DD-ceiling inputs + Reset-to-defaults button. Frontend defaults match the backend (0.50 / 0.50, -0.25, -0.40). Schema `WalkForwardRunRequestSchema` accepts `score_weights` + `ship_thresholds` (passthrough). +5 new backend tests for max_dd gates + tunable defaults. Backend total: 1159 → 1164.
- 2026-04-27 19:50:00 -04:00 · Claude · schema-added · `backend/app/domain/strategy.py` — new `LogicalExitRule` + `LogicalExitRuleKind {feature_condition, bars_since_entry, time_in_position_seconds, time_of_day_et, minutes_before_session_close, session_window, hybrid}` per operator doctrine. `SignalRule.condition` made optional; new `SignalRule.logical_exit_rule` carries time/bar/session/hybrid payloads. Validator: rule requires at least one of (condition, logical_exit_rule); entry rules cannot carry logical_exit_rule. **Doctrine:** `logical_exit` is the only exit intent — time / bar / session / feature / hybrid all map under it; never a sibling top-level intent. SignalPlanIntent vocabulary unchanged (guard test added).
- 2026-04-27 19:50:00 -04:00 · Claude · schema-added · `backend/app/domain/signal_plan.py` — `SignalPlanLogicalExit.rule: str` evolved to `SignalPlanLogicalExit.rule: LogicalExitRule` (typed payload). `SignalPlanLogicalExit.action` defaults to `CLOSE`. Existing `test_signal_plan_contracts` test updated to construct typed payload.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `SignalEngine.evaluate(strategy, snapshot, *, position_contexts: dict[str, PositionContext] | None = None)` — new `position_contexts` kwarg (back-compat default `None`). Exit rules require `position_contexts[symbol].has_position == True` to fire. New `PositionContext` dataclass exported from `backend.app.decision`. Entry-rule path unchanged. New `_evaluate_logical_exit_rule` evaluates BARS_SINCE_ENTRY / TIME_IN_POSITION_SECONDS / TIME_OF_DAY_ET / MINUTES_BEFORE_SESSION_CLOSE / SESSION_WINDOW / HYBRID against the context (ET timezone via `zoneinfo.ZoneInfo("America/New_York")`). When both `condition` and `logical_exit_rule` are present they AND together; HYBRID can express OR explicitly via `operator='any'`.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `SignalPlanBuilder.build_from_candidate(...)` — accepts `logical_exit_rule`, `logical_exit_action` (default CLOSE), `logical_exit_quantity_pct`, `logical_exit_scope`. Exit candidates produce `intent=SignalPlanIntent.LOGICAL_EXIT` carrying a typed `SignalPlanLogicalExit` payload (no other top-level intent for time/bar/session exits). OPEN candidates unchanged.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `RiskResolver.decide(...)` — non-OPEN intents (LOGICAL_EXIT / CLOSE / REDUCE / TARGET / STOP / TRAIL / BREAKEVEN / RUNNER) routed to a new `_decide_exit` path: sizing comes from `account_state.existing_position_quantity` × `exit_quantity_pct` (default 100%), not from RiskPlan.fixed_shares / risk_per_trade_pct. Card carries `lifecycle_intent` from the SignalPlan, `sizing_method=exit_from_position:N%`, full `calculation_steps` trace, decision=APPROVED when there is a position to close, SKIPPED otherwise.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `HistoricalReplayEngine` — bar loop tracks per-symbol `bar_index_by_symbol`; before each `SignalEngine.evaluate(...)` builds `position_contexts` from the position ledger (entry timestamp + entry bar index recorded on `submit_open_order`). EXIT candidates route through new `_handle_exit_intent`: pure feature exits wrap their `condition` as `LogicalExitRule(kind=FEATURE_CONDITION, ...)`; structured exits use the rule's `logical_exit_rule` directly. SignalPlan→RiskResolver.decide→`SimulatedBroker.submit_close_order` (new method) emits a CLOSE order with full `risk_decision_id` + `signal_plan_id` + `risk_plan_version_id` lineage on the resulting fill / trade. `SimulatedPositionLedger` extended with `record_entry_lineage` / `entry_signal_plan_id` / `entry_bar_index` so exit cards reference the opening SignalPlan.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `RuntimeOrchestrator.process_bar` (live runtime) — also passes `position_contexts` to `SignalEngine.evaluate`, derived from `DeploymentPositionManager.positions_for_deployment()` (cached per-bar to keep `position_reader` query count at one). Live runtime now exercises the same exit-rule semantics Sim Lab + Backtest do.
- 2026-04-27 19:50:00 -04:00 · Claude · route-changed · `backend/app/features/planner.py` — `_strategy_feature_refs` now also walks `SignalRule.logical_exit_rule` (FEATURE_CONDITION + HYBRID children) so the FeaturePlan includes feature refs embedded inside logical_exit payloads.
- 2026-04-27 19:50:00 -04:00 · Claude · lint · `backend/tests/unit/simulation/test_logical_exit_spine.py` (new, 5 tests) — bars_since_entry exit, time_in_position exit, hybrid (bars AND feature) exit, feature-condition-only exit (asserts no premature firing), and a doctrine guard asserting `SignalPlanIntent` enum stays at `{open, close, reduce, target, stop, trail, breakeven, runner, logical_exit}`. Verifies trades carry `risk_decision_id` + `signal_plan_id` + `risk_plan_version_id` lineage on signal-driven CLOSE fills.
- 2026-04-27 19:50:00 -04:00 · Claude · coordination · 1141 / 1141 backend unit tests pass; frontend typecheck + 63 / 63 vitest + banned-name lint all clean. LOCKS released.
- 2026-04-27 19:05:00 -04:00 · Claude · schema-added · `backend/app/domain/risk_decision_card.py` (new) — `RiskDecisionCard` + `RiskCalculationStep` + `RiskDecisionMode {backtest, sim_lab, broker_paper, broker_live, walk_forward, optimization}` + `RiskDecisionStatus {approved, rejected, reduced, capped, skipped, requires_operator}` per `RISK_PLAN_SIGNALPLAN_BACKTEST_BACKEND_CONTRACT.md` §5. SQLite table `risk_decision_cards` indexed by run_id / signal_plan_id / account_id / strategy_version_id; SQLite table `historical_datasets` keyed on (provider, symbol, timeframe, adjustment_policy).
- 2026-04-27 19:05:00 -04:00 · Claude · route-added · `GET /api/v1/risk-decisions/{risk_decision_id}` and `GET /api/v1/risk-decisions?run_id|signal_plan_id|account_id|strategy_version_id=…` — read API per contract §8.3.
- 2026-04-27 19:05:00 -04:00 · Claude · route-added · `POST /api/v1/data-center/historical-datasets/ingest` — operator-driven Alpaca / Yahoo bar ingest with `adjustment_policy ∈ {split_dividend_adjusted, split_only, raw}` + cache-hit invariant (matching `(provider, symbol, timeframe, [start,end] ⊆ stored, adjustment_policy)` makes zero provider calls). Per-dataset metadata captures `provider`, `symbol`, `timeframe`, `start/end`, `adjustment_policy`, `timezone`, `ingested_at`, `source_request_parameters`, `data_quality_warnings`.
- 2026-04-27 19:05:00 -04:00 · Claude · route-changed · `POST /api/v1/research/backtests` — request schema accepts `risk_plan_version_id` (required when `symbols` is supplied per contract §6.1), `cost_model {commission_per_trade, slippage_bps}` (post-fill metrics-only), `source ∈ {yahoo, alpaca}`, `monte_carlo {enabled, method, replications, block_size, seed}`. Synthetic engine retired: `BacktestExecutionService` now drives `HistoricalReplayEngine` (mode=backtest), which calls `BatchFeatureEngine` + `SignalEngine` + `SignalPlanBuilder` + `RiskResolver.decide` + `SimulatedBroker`; every fill / trade row carries `risk_decision_id` + `signal_plan_id` + `risk_plan_version_id`. New post-fill `BacktestMetricsService` computes the 11 standard metrics + per-regime via existing `RegimeClassifier`. New `MonteCarloAnalyzer` (trade-PnL bootstrap + block bootstrap on bar returns, deterministic seed). The legacy bare `POST /api/v1/research/backtests` path that recorded a placeholder run shape remains for tests that do not provide `symbols`.
- 2026-04-27 19:05:00 -04:00 · Claude · route-changed · `HistoricalReplayEngine.run` (Sim Lab + Backtest spine) — `_size_order` deleted; engine now constructs `SignalPlanBuilder` + `RiskResolver` per intent, emits a `RiskDecisionCard` per sized SignalPlan, and threads `risk_decision_id` / `signal_plan_id` / `risk_plan_version_id` through `SimulatedOrder`, `SimulatedFill`, `SimulatedTrade`. `mode` parameter (default `sim_lab`) tags emitted cards. Optional `risk_decision_sink` injects card persistence; backtest path persists via `SQLiteRuntimeStore`. Sim Lab inherits the spine for free — its existing tests still pass and its trade ledger now records full lineage.
- 2026-04-27 22:35:00 -04:00 · Codex · migration · `backend/app/persistence/{models.py,runtime_store.py}` + `backend/app/broker_accounts/models.py` — B2 RiskPlan persistence landed: `risk_plans` and `risk_plan_versions` SQLite tables/indexes, store save/load/list methods, `BrokerAccount.default_risk_plan_id/default_risk_plan_version_id`, migration coverage; backend unit suite 1230/1230 passing.
- 2026-04-27 22:28:16 -04:00 · Codex · schema-added · `backend/app/domain/risk_plan.py` — B1 RiskPlan domain contract landed: product-facing `RiskPlan`, `RiskPlanVersion`, `RiskPlanConfig`, status/tier/source/sizing enums, canonical config fingerprinting, and legacy `RiskProfileVersion` adapter; backend unit suite 1227/1227 passing.
- 2026-04-27 19:05:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/Backtests.tsx` + `frontend/src/components/backtests/{RunBacktestDrawer,MonteCarloCard,RiskDecisionCardDrawer}.tsx` (new) + `frontend/src/api/{researchRuns,riskDecisions}.ts` + `frontend/src/api/schemas/{researchRuns,riskDecisions}.ts` — Run-new-backtest drawer (Strategy + Strategy version + Risk Plan version id + symbols + start/end + timeframe + initial capital + cost model + Yahoo/Alpaca + collapsible Monte Carlo with method/replications/block_size/seed); MonteCarloCard rendering p05/p25/p50/p75/p95 bands for terminal equity / Sharpe / max-DD + final-equity histogram bars; RiskDecisionCardDrawer (per contract §9.6 — decision summary + inputs + step-by-step `calculation_steps` table + constraints/violations/warnings + lineage). Trade-ledger rows are click-through to the RiskDecisionCard drawer when `risk_decision_id` is present. Frontend typecheck + 63 vitest tests + banned-name lint all green.
- 2026-04-27 19:05:00 -04:00 · Claude · lint · `backend/tests/unit/api/test_frontend_api_contract.py` — registered `POST /api/v1/data-center/historical-datasets/ingest`, `GET /api/v1/risk-decisions`, `GET /api/v1/risk-decisions/{risk_decision_id}`. Full backend unit suite 1136/1136 passing.
- 2026-04-27 16:05:00 -04:00 · Claude · coordination · `COORDINATION/INBOX_CODEX.md` — filed `request` for the Backtest spine slice (replace synthetic engine; add SignalPlanBuilder + RiskResolver.decide + RiskDecisionCard wiring inside HistoricalReplayEngine; Data Center ingest with cache-hit invariant; Monte Carlo). Plan: `C:\Users\potij\.claude\plans\melodic-tinkering-tulip.md`. Doctrine: `RISK_PLAN_SIGNALPLAN_BACKTEST_BACKEND_CONTRACT.md`. Walk-Forward / Optimization / full Risk Plans CRUD UI explicitly out of scope. Awaiting Codex decision (a/b/c) and RiskDecisionCard + Backtest request schema before frontend scaffolding.
- 2026-04-27 15:42:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/SimLab.tsx` + `frontend/src/components/charts/SimLabReplayChart.tsx` (new) + `frontend/src/api/researchRuns.ts` + `frontend/src/api/schemas/researchRuns.ts` + `backend/tests/unit/api/test_frontend_api_contract.py` — Sim Lab streaming view (gate C4): WS `/api/v1/research/sim_lab/stream` consumer renders one `lightweight-charts` candle chart per symbol with entry (green up-arrow below bar) and exit (red down-arrow above bar) markers per virtual fill, equity curve overlaid on a separate price scale, inline KPI strip (Universe / Window / Bars / SignalPlans / Fills / Last equity / Last update). Stacked positions / fills / equity-sparkline tables retired per operator feedback "see what is going on on charts not fill up my screen". `SimLabStreamMessageSchema` added with `.passthrough()`; readers handle both nested (pre-15:33) and flattened (Codex 15:33 LEDGER) virtual_fill / position / equity payloads. Persistent `websocket_error` banner suppressed once `session_completed` arrives; previously the WS close handler emitted a misleading warning even on clean completion.
- 2026-04-27 15:11:00 -04:00 · Claude · feedback-memory · `memory/feedback_research_visualizations_chart_first.md` — saved doctrine: research surfaces (Sim Lab / Backtests / Walk-Forward / Chart Lab streaming) lead with charts; tables are drill-downs only. Reference component: `SimLabReplayChart`.
- 2026-04-27 15:36:00 -04:00 · Codex · coordination · `memory/MEMORY.md`, `COORDINATION/INBOX_CLAUDE.md` — recorded operator feedback that Sim Lab streaming must be chart-first, with candles/fill markers/equity overlay as primary UI; left C4 unticked until Claude ships visible frontend evidence.
- 2026-04-27 15:33:08 -04:00 · Codex · route-changed · `WS /api/v1/research/sim_lab/stream` — stream is now transient visualization telemetry: it no longer persists Sim Lab session rows, stays open after `session_completed` until the browser closes it, and flattens virtual fill/position/equity fields for chart/timeline rendering.
- 2026-04-27 15:23:00 -04:00 · Codex · route-changed · `POST /api/v1/strategies/{strategy_id}/versions/{version_id}/freeze` — freeze now rejects draft-only verification versions until the StrategyVersion is attached to at least one Deployment; research can verify drafts, Deployment attachment is the commit boundary for freezing.
- 2026-04-27 15:13:00 -04:00 · Codex · route-added · `WS /api/v1/research/sim_lab/stream` — Sim Lab stream mode now emits ordered `session_started`, `bar`, `signal_plan`, `virtual_fill`, `position`, `equity`, and `session_completed` messages over deterministic replay evidence.
- 2026-04-27 15:13:00 -04:00 · Codex · schema-added · `backend.app.research.sim_lab.SimLabStreamMessage` — stream payload contract includes `type`, `sequence`, `run_id`, `timestamp`, and additive JSON payload with simulation-only SignalPlan refs and virtual fill/position/equity artifacts.
- 2026-04-27 15:11:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/Operations.tsx` + `frontend/src/api/operations.ts` + `frontend/src/api/schemas/operations.ts` — `BrokerOrderTraceCard` consumes Codex's 14:53 `GET /api/v1/operations/broker-orders/{broker_order_id}`: operator pastes an Alpaca broker order id, gets internal-order detail (symbol, side, qty, status, broker_status, fills, last sync). `OrderDetailSchema` added with `.passthrough()`. 404 surfaces an explicit "no match" warning rather than silent failure. `Operations.test.tsx` extended to 4 tests.
- 2026-04-27 15:11:00 -04:00 · Claude · lint · `backend/tests/unit/api/test_frontend_api_contract.py` — registered `GET /api/v1/operations/broker-orders/{broker_order_id}` in expected route set (matches Codex 14:53 LEDGER entry).
- 2026-04-27 15:11:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/SimLab.tsx` — Sim Lab batch-run version selector no longer filters to frozen-only. Drafts and frozen versions both selectable; status badge shown inline. Per operator doctrine: research surfaces verify drafts; freeze is the commit-to-deploy step.
- 2026-04-27 14:53:56 -04:00 · Codex · route-added · `GET /api/v1/operations/broker-orders/{broker_order_id}` — Operations can now trace an Alpaca broker order id directly to internal order detail; `OrderDetail.deployment_id` is nullable for manual operator orders.
- 2026-04-27 15:02:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/SimLab.tsx` + `frontend/src/api/researchRuns.ts` + `frontend/src/api/schemas/researchRuns.ts` — Sim Lab "Run batch sim" drawer wired to Codex's new `POST /api/v1/research/sim_lab/runs`: strategy + frozen-version selectors gated on `StrategiesApi.listVersions`, scenario name + universe + timeframe + start/end + initial cash + bar-count form, response invalidates the sessions list. `SimLabBatchRunResponseSchema` + `SimLabBatchRunRequestSchema` added with `.passthrough()` on the response. `SimLab.test.tsx` extended to 5 tests (drawer trigger enabled when ≥1 strategy loaded).
- 2026-04-27 15:02:00 -04:00 · Claude · lint · `backend/tests/unit/api/test_frontend_api_contract.py` — registered `POST /api/v1/research/sim_lab/runs` in expected route set (matches Codex 14:41 LEDGER entry).
- 2026-04-27 14:41:22 -04:00 · Codex · route-added · `POST /api/v1/research/sim_lab/runs` — Sim Lab batch create-run endpoint now executes deterministic fixed-window historical replay and returns persisted `SimulationRunEvidence` plus events/orders/fills/positions/trades/equity_curve.
- 2026-04-27 14:54:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/WalkForward.tsx` (rewrite from `ResearchEvidencePage` stub) + `WalkForward.test.tsx` — Walk-Forward results summary (gate E6): runs table with strategy display name + folds-passed ratio + recommend/reject tone-badge; detail view summary KpiCards (median OOS Sharpe, OOS-vs-IS decay, regime fit score, folds passed); per-fold IS/OOS, parameter stability heatmap, OOS regime breakdown, equity-curve-with-IS/OOS-shading panels scaffolded behind `AwaitingApiOrError` pinned to `/api/v1/walk-forward/runs/{run_id}/{folds,parameter-stability,oos-regime-breakdown,equity-curve}`. Summary fields read additive keys off `metrics` JsonDict (`median_oos_sharpe`, `oos_vs_is_decay`, `regime_fit_score`, `recommendation`).
- 2026-04-27 14:46:00 -04:00 · Claude · frontend-consumed · `frontend/src/components/cards/ChartLabHubCard.tsx` (new) + `frontend/src/lib/chartLabPin.ts` (new) + `frontend/src/routes/Dashboard.tsx` + `frontend/src/routes/ChartLab.tsx` — Chart Lab dashboard hub pin (gate D7): tab-local localStorage pin, Pin/Unpin button on Chart Lab while a session is streaming, hub card on the Dashboard subscribes to `/api/v1/chart-lab/stream?symbol=…` for the pinned symbol, `PulseDot` tone + pulse reflects WS health (open+bar=ok-pulse, open+no-bar=info-pulse, connecting=info, reconnecting=warn, error=danger, no-pin=muted), last-bar close + last-update relative time rendered, error banner inline. `ChartLabHubCard.test.tsx` 3 tests including the unpin click path.
- 2026-04-27 14:34:33 -04:00 · Codex · route-changed · `GET /api/v1/operations/overview` — `open_orders_count` now aggregates BrokerSync-persisted broker open orders from Account summaries, so external Alpaca open orders are visible in the overview total.
- 2026-04-27 14:38:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/SimLab.tsx` (rewrite from `ResearchEvidencePage` stub) — Sim Lab first-class route + side-by-side compare (gate C6): sessions table reads `/api/v1/sim-lab/sessions` (passthrough), strategy display name lookup via `/api/v1/strategies`, two-session selector with disabled-cross-pick, Compare cards with diff highlighting on scenario / strategy / window / created and signed `(±N)` deltas on SignalPlan / Sim order / Sim fill counts, additive metrics block with `vs` inline. Streaming sim WebSocket + pause/step/resume controls scaffolded behind a banner pinned to `RESEARCH_CREATE_RUN_API_HANDOFF.md`.
- 2026-04-27 14:30:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/Backtests.tsx` + `frontend/src/api/researchRuns.ts` + `frontend/src/api/schemas/researchRuns.ts` + `frontend/src/components/charts/Sparkline.tsx` — Backtests results page (gate B7 + B8): list + detail with run overview, metric cards (CAGR, Sharpe, Sortino, Calmar, max DD, hit rate, profit factor, expectancy, exposure, turnover, time-in-market, cost model), equity-curve sparkline, drawdown sparkline, per-regime metric table, per-symbol breakdown, trade ledger. Strategy display name + ticker symbols as primary labels (no UUIDs). All schemas `.passthrough()`; status / state enums loosened to `z.string()`.
- 2026-04-27 14:30:00 -04:00 · Claude · frontend-consumed · `frontend/src/api/researchRuns.ts` — `BacktestsApi` migrated to canonical `/api/v1/research/backtests` namespace; added `results()` and `metrics()` clients targeting Codex's evidence endpoints.
- 2026-04-27 14:24:20 -04:00 · Codex · coordination · `backend/app/brokers/alpaca.py`, `backend/app/runtime/runtime_context.py` — fixed Account open-order polling source: Alpaca uses explicit `status=open`, non-terminal open statuses are preserved, REST reconcile starts even if stream startup fails, and boot no longer marks sync fresh without real reconcile.
- 2026-04-27 14:30:00 -04:00 · Claude · lint · `backend/tests/unit/api/test_frontend_api_contract.py` — registered `GET/POST /api/v1/research/backtests`, `GET /api/v1/research/backtests/{run_id}{,/cancel,/results,/metrics}` and `PATCH /api/v1/strategies/{strategy_id}/versions/{version_id}` in expected route set (matches Codex 13:55 + 14:07 LEDGER entries).
- 2026-04-27 14:14:25 -04:00 · Codex · coordination · `Operations_Turtle_Shell_Artifacts/NEXT_IMPLEMENTATION_SEQUENCE.md` — added Slice 11 Account Trade Sync Reconciliation Scheduler; Claude notified that stream health and REST reconciliation freshness must remain separate.
- 2026-04-27 14:15:00 -04:00 · Claude · frontend-consumed · `frontend/src/routes/StrategyDetail.tsx` + `frontend/src/routes/Strategies.tsx` + `frontend/src/router.tsx` — Strategy authoring slice (gate A1–A5 frontend): first-class `/strategies/:strategyId` page with overview, latest-published lineage card, versions table joined to `/api/v1/deployments` (deployments-using-each-version), Add Version drawer, Edit Draft drawer behind `AwaitingApiOrError` for the pending PATCH route, type-name-to-confirm Delete, Deprecate.
- 2026-04-27 14:15:00 -04:00 · Claude · schema-added · `frontend/src/api/schemas/strategies.ts` + `frontend/src/api/strategies.ts` — `StrategyVersionRecord.frozen_by` optional field for publisher attribution; status enums loosened to `z.string()`; `StrategySchema`, `StrategyVersionRecordSchema`, `StrategyResponseSchema`, `StrategyListResponseSchema` `.passthrough()`; `editDraftVersion` PATCH client targeting the awaiting `PATCH /api/v1/strategies/{strategy_id}/versions/{version_id}` route.
- 2026-04-27 14:07:44 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md` — released Codex strategy/research leases after B1-B6, F1-F2, and A3-A4 backend slices verified.
- 2026-04-27 14:07:44 -04:00 · Codex · route-added · `PATCH /api/v1/strategies/{strategy_id}/versions/{version_id}` — draft StrategyVersion edit route added; frozen versions reject with `strategy_version is frozen and cannot be edited`.
- 2026-04-27 14:07:44 -04:00 · Codex · schema-added · `backend.app.strategies.StrategyVersionRecord.frozen_by` — freeze route now persists optional `X-Operator-Session-Id` publisher attribution.
- 2026-04-27 14:02:06 -04:00 · Codex · schema-added · `backend/app/research/regimes/classifier.py` and `BacktestRun.results.regime_tags/per_regime_metrics` — deterministic cached regime labels now join into backtest evidence and per-regime metrics.
- 2026-04-27 13:55:10 -04:00 Â· Codex Â· route-added Â· `POST/GET /api/v1/research/backtests`, `GET /api/v1/research/backtests/{run_id}/results`, `GET /api/v1/research/backtests/{run_id}/metrics` â€” backtest create/status/results/metrics routes now produce durable research evidence with universe, capital, and cost model inputs.
- 2026-04-27 13:55:10 -04:00 Â· Codex Â· schema-added Â· `backend.app.domain.BacktestRun` â€” added additive backtest evidence fields: universe, timeframe, initial_capital, cost_model, status, status_history, and results.

- 2026-04-27 13:35:00 -04:00 Â· Claude Â· coordination Â· `COORDINATION/` â€” bootstrapped inter-agent protocol (PROTOCOL, LOCKS, INBOXES, LEDGER); Codex must read on next turn.
- 2026-04-27 13:02:54 -04:00 Â· Claude Â· route-changed Â· `backend/app/operations/service.py` â€” surgical filter of `deployment_id=None` in `get_account_operations`; resolves 503 after manual orders. No payload shape change.
- 2026-04-27 13:02:54 -04:00 Â· Claude Â· schema-added Â· `frontend/src/api/schemas/operations.ts` â€” `BrokerPositionSnapshotSchema` accepts both `qty`/`quantity` and `avg_entry_price`/`average_entry_price`; status enums loosened to `z.string()`; major schemas `.passthrough()`.
- 2026-04-27 13:02:54 -04:00 Â· Claude Â· frontend-consumed Â· `frontend/src/routes/OperationsLedger.tsx` â€” persistent aggregated Orders + Positions cards on Operations page (5s polling, per-account `useQueries` fan-out).
- 2026-04-27 13:02:54 -04:00 Â· Claude Â· frontend-consumed Â· `frontend/src/routes/ManualOrderDrawer.tsx` â€” drawer no longer auto-closes; inline result banner; idempotency key rotates on success; force-refetch operations + accounts queries.
- 2026-04-28 22:30:00 -04:00 · Claude · slice-shipped · `frontend/src/components/strategy_builder/wizard/templates.ts` + `templates.test.ts` — Slice 6e (Sonnet 4.6 under Opus 4.7 coordination): curated starter catalog completeness pass. Audited all 12 templates against shipped backend + WizardIntent schema; pruned 2 spurious required_feature_families (`vwap-reclaim` "session-low", `connors-rsi-2` "ROC" — neither was actually seeded); enriched intent_keywords across all 12 for fuzzier intent extraction. Added per-template integration smoke (R5 seeding contract): AIComposerRequestSchema parse + R3 direction/horizon/timeframe fidelity for each of the 9 ready templates; deferred-state assertions for the 3 SESSION-deferred templates. Tests 14 → 73; full vitest 277/277 passed; typecheck + lint:names green.
- 2026-04-29 00:39:19 -04:00 · Codex · frontend-consumed · `frontend/src/api/{screener,watchlists}.ts`, `frontend/src/api/schemas/{screener,watchlists}.ts`, `frontend/src/routes/{Screeners,ScreenerDetail,Watchlists,Deployments}.tsx`, `frontend/src/components/screener/` — Completed operator override Alpaca-first Screener/Watchlist UI: Alpaca market-list entry, templates, AI advisory composer with visible typed expression tree, run/rerun/compare, archive/delete confirmation, static/dynamic save-as-watchlist, refresh evidence/diff, readable Deployment strategy/Watchlist labels, and expression-preserving versioning. No broker submit or trading spine shortcut added.
- 2026-04-29 00:39:19 -04:00 · Codex · schema-added · `backend/app/screener/{domain,service}.py`, `backend/app/api/routes/screener.py` — Hardened release findings: `source_preference` vocabulary is now `auto|alpaca|data_center` (no production Yahoo option) and `run_screener(version_id=...)` rejects versions from another Screener to preserve lineage/audit.
- 2026-04-29 00:39:19 -04:00 · Codex · lint · `frontend/src/routes/{Screeners,ScreenerDetail,Watchlists,Deployments}.test.tsx`, `backend/tests/unit/screener/{test_screener_service.py,test_screener_routes.py}`, `backend/tests/unit/api/test_frontend_api_contract.py` — Final verification: `npm.cmd run typecheck` passed; targeted frontend route tests 14 passed; targeted backend Alpaca-first/Watchlist/contract tests 39 passed; full frontend `npm.cmd test` 282 passed plus banned-name lint clean; `npm.cmd run build` passed with Vite chunk-size warning only; full backend unit suite 1376 passed, 6 warnings. No opt-in live Alpaca credential call was run.
- 2026-04-29 00:39:19 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md`, `COORDINATION/INBOX_CLAUDE.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Released operator-override frontend/shared leases and closed the Alpaca Expert, UX reviewer, and Enemy Agent retrospective loop after fixing rejection findings.
- 2026-04-29 00:45:14 -04:00 · Codex · operations-fix · local backend process on `127.0.0.1:8000` — Reproduced operator-reported Screeners 500 against stale live process; current code returned 200 under `TestClient`. Restarted backend so the live server loads the current Screener/Watchlist schema/migration code. Verified real HTTP 200 for `/api/v1/screeners`, `/api/v1/watchlists`, `/api/v1/screeners/templates`, `/api/v1/screeners/market-lists`, and `/api/v1/screeners/fields`. No source code changes.
- 2026-04-29 01:44:20 -04:00 · Codex · route-changed · `backend/app/screener/runtime.py`, `backend/app/screener/sources.py` — Headless release walkthrough found and Codex fixed live Alpaca provider gaps: Screener runtime no longer imports stale `DataCenterStore`, persisted Alpaca credentials resolve through the runtime SQLite store, historical bar lookup uses the same composition root, and Alpaca market-list calls now respect the `top <= 50` provider limit. Provider pack remains outside the trading spine; no broker truth writer changed.
- 2026-04-29 01:44:20 -04:00 · Codex · lint · `backend/tests/unit/screener/test_screener_runtime.py`, `frontend/scripts/headless-screener-watchlist.mjs`, `frontend/package.json` — Added repeatable `npm.cmd run headless:screener` browser walkthrough and provider regression tests. Verification: headless walkthrough passed 17 checks across Screeners, Alpaca market lists, templates, AI advisory composer, run/rerun/compare, static/dynamic Watchlist save and refresh, Deployment entry universe, archive/delete guard, readable labels, and audit/source visibility; `python -m pytest backend/tests/unit/screener/test_screener_runtime.py backend/tests/unit/screener/test_screener_alpaca_first.py backend/tests/unit/screener/test_screener_service.py backend/tests/unit/screener/test_screener_routes.py -q` -> 30 passed; `npm.cmd run typecheck` passed; `node --check scripts/headless-screener-watchlist.mjs` passed; `npm.cmd run lint:names` clean.
- 2026-04-29 01:44:20 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md`, `COORDINATION/INBOX_CLAUDE.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Released headless verification leases and notified Claude. No broker submit/order path was exercised; live Alpaca reads stayed inside the Screener provider boundary.
- 2026-04-29 02:54:40 -04:00 · Codex · frontend-consumed · `frontend/src/routes/Deployments.tsx` — Deployment creation UI now accepts the current Strategy version instead of requiring a frozen Strategy version. This aligns the UI with the established freeze doctrine: Deployment attachment is the commit boundary before freezing. Deployment cards still prefer readable Strategy and Watchlist names over ids.
- 2026-04-29 02:54:40 -04:00 · Codex · lint · `frontend/scripts/headless-screener-watchlist.mjs`, `frontend/src/routes/Deployments.test.tsx` — Enemy Agent objection closed. Headless verifier now passes 21 checks with UI drawer/control coverage for AI Composer, Day Losers and Most Active market-list variants, typed criteria controls, Deployment Strategy version selection, and the full Screener -> Watchlist -> Deployment flow. Verification: `npm.cmd run headless:screener` -> 21 checks passed; `npm.cmd run typecheck` passed; `npx.cmd vitest run src/routes/Deployments.test.tsx` -> 4 passed; `npm.cmd run lint:names` clean; focused backend Screener suite -> 30 passed, 5 warnings.
- 2026-04-29 02:54:40 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md`, `COORDINATION/INBOX_CLAUDE.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Released verifier-hardening leases. Final Alpaca Expert, UX reviewer, and Enemy Agent retrospectives all pass with no blockers.
- 2026-04-29 04:03:49 -04:00 · Codex · route-added · `backend/app/api/routes/discovery_schedules.py`, `backend/app/api/server.py` — Added durable discovery schedule API plus startup/shutdown poller wiring for due Screener runs and Watchlist refresh snapshots. Schedules target exact ScreenerVersion ids or Watchlist ids, record executions, guard overlap/delete, and keep broker/order/position spine untouched.
- 2026-04-29 04:03:49 -04:00 · Codex · schema-added · `backend/app/screener/{schedules.py,schedule_store.py,schedule_service.py,scheduler_runtime.py,domain.py}` — Added persisted schedule/execution models, SQLite store, cadence/next-run planner, run-now/run-due service, pause/resume/archive/delete lifecycle, Watchlist active-Deployment approval guard, and `run_kind=scheduled` lineage.
- 2026-04-29 04:03:49 -04:00 · Codex · bug-fix · `backend/app/screener/{sources.py,service.py}`, `frontend/src/components/screener/ResultsTable.tsx` — Alpaca capability evidence now survives bar metric failures; unavailable provider evidence is displayed as unavailable, not false "not tradeable." Live Alpaca asset lookup confirmed AAPL active/tradable/fractionable/shortable/easy-to-borrow.
- 2026-04-29 04:03:49 -04:00 · Codex · frontend-consumed · `frontend/src/api/{discoverySchedules.ts,schemas/discoverySchedules.ts}`, `frontend/src/components/screener/DiscoveryScheduleControls.tsx`, `frontend/src/routes/{ScreenerDetail.tsx,Watchlists.tsx}` — Added schedule controls for Screener detail and Watchlist detail with readable target labels, cadence, approval policy, last/next run, last error, run-now, pause/resume, archive, and delete flows.
- 2026-04-29 04:03:49 -04:00 · Codex · lint · `backend/tests/unit/screener/{test_discovery_schedules.py,test_screener_alpaca_first.py}`, `backend/tests/unit/api/test_frontend_api_contract.py`, `frontend/scripts/headless-screener-watchlist.mjs`, `frontend/src/components/screener/ResultsTable.test.tsx` — Verification passed: backend unit suite 1391 passed; focused backend Screener/Watchlist/API/guardrail set 58 passed; frontend full suite 285 passed plus banned-name lint clean; `npm.cmd run headless:screener` passed 23 checks including schedules.
- 2026-04-29 04:03:49 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md`, `COORDINATION/INBOX_CLAUDE.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Released scheduling/capability leases, notified Claude, and closed Alpaca Expert, UX reviewer, and Enemy Agent retrospective with no blockers.
- 2026-04-29 04:35:37 -04:00 · Codex · docs-added · `Operations_Turtle_Shell_Artifacts/SCREENER_WATCHLIST_USER_JOURNEY.md` — Drafted full Screener/Watchlist journey for Nanyel/operator, expert day trader, and swing/quant user, including doctrine, persona steps, pass conditions, and headless acceptance gates.
- 2026-04-29 04:35:37 -04:00 · Codex · route-hardened · `backend/app/screener/{schedule_store.py,schedule_service.py}`, `backend/tests/unit/screener/test_discovery_schedules.py` — Discovery schedule execution now atomically claims running executions and abandons stale running executions after timeout with audit evidence; backend discovery schedule tests now cover stale-running recovery.
- 2026-04-29 04:35:37 -04:00 · Codex · frontend-consumed · `frontend/src/components/screener/{DiscoveryScheduleControls.tsx,DiscoveryScheduleControls.test.tsx}` — Schedule controls now expose timezone and execution history with readable run/snapshot labels, diff counts, trigger/status/error; tests cover execution history, visible Watchlist schedule creation, and visible run/pause/archive controls.
- 2026-04-29 04:35:37 -04:00 · Codex · lint · `frontend/scripts/headless-screener-watchlist.mjs` — Headless Screener/Watchlist walkthrough now passes 43 checks with explicit `operator`, `day_trader`, and `swing_quant` journey gates, AAPL tradability regression, UI-driven Watchlist schedule creation, and visible schedule action clicks.
- 2026-04-29 04:35:37 -04:00 · Codex · lint · Verification passed: `npm.cmd run headless:screener` -> 43 checks; `npm.cmd test` -> 288 passed plus banned-name lint clean; `npm.cmd run typecheck` passed; `node --check scripts/headless-screener-watchlist.mjs` passed; `python -m pytest backend/tests/unit -q` -> 1392 passed, 6 warnings.
- 2026-04-29 04:35:37 -04:00 · Codex · coordination · `COORDINATION/LOCKS.md`, `COORDINATION/INBOX_CLAUDE.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Released persona journey/headless leases and notified Claude. Enemy/UX/test-mapper retrospective findings were fixed before closeout.
- 2026-04-29 06:28:40 -04:00 · Codex · git · `9e1d3d2` — Created local checkpoint commit `Checkpoint production rebuild and screener journey` on `master`, capturing the verified rebuild, Alpaca-first Screener/Watchlist journey, frontend migration, backend services, tests, docs, and coordination artifacts. Excluded `.env`, `.claude/`, `.runtime_logs/`, `__pycache__`, `.pyc`, and `*.tsbuildinfo` from the commit.
- 2026-04-29 06:28:40 -04:00 · Codex · coordination · `COORDINATION/LEDGER.md`, `COORDINATION/INBOX_CLAUDE.md`, `COORDINATION/LOCKS.md`, `Operations_Turtle_Shell_Artifacts/OPERATION_STATUS.md` — Recorded Git checkpoint state. GitHub push is blocked until a remote URL is configured; `git remote -v` returned no remotes.
- 2026-04-29 06:38:57 -04:00 · Codex · git · `c3a83ce` — Added missing Chart Lab strategy preview source commit after status audit found `StrategyPreviewChart.tsx`, `ChartLab.tsx`, and `ChartLab.test.tsx` were still outside the checkpoint. Kept preview Alpaca-only in the UI, fixed `lightweight-charts` RGB parsing, and verified `npm.cmd run typecheck` plus `npx.cmd vitest run src/routes/ChartLab.test.tsx` -> 7 passed.
- 2026-04-29 07:28:47 -04:00 · Codex · git · `0b738a8` — Stopped tracking local `.env` and Python bytecode artifacts, added `.claude/` to `.gitignore`, and left local files on disk ignored. This reduces local dirty status from generated/secret artifacts without changing runtime source behavior.
